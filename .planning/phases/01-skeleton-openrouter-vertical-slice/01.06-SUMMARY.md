---
phase: 01-skeleton-openrouter-vertical-slice
plan: "06"
subsystem: ui
tags: [swiftui, menubarextra, observable, environment, quota-bar, timeline-view, ui-02, b4, b5, swift-testing]
dependency_graph:
  requires:
    - 01.01-SUMMARY.md (AgentsUsageBar.xcodeproj, PopoverRootView/ProviderRowView/FooterView Wave 0 stubs)
    - 01.02-SUMMARY.md (Quota, ProviderStatus, ProviderState, UsageSnapshot, DailyTotals, Clock/SystemClock)
    - 01.05-SUMMARY.md (AggregateStore @Observable @MainActor — providers, totals, refresh(now:), seedPlaceholder)
  provides:
    - PopoverRootView (TotalsHeaderView + sorted ProviderRowViews + FooterView; .frame(width:360); POLL-03 .task on-open refresh)
    - TotalsHeaderView (today total tokens + USD from store.totals, UI-01)
    - ProviderRowView (StatusDot + name + tokens/USD/balance + QuotaBar + RelativeTimestampLabel + Resets —)
    - FooterView (Refresh now Cmd-R calling store.refresh(now: clock.now()); Quit Cmd-Q; @Environment(\.clockService) consumer)
    - QuotaBar (color-coded progress bar with internal static color(forFraction:) test seam, B4/UI-02)
    - StatusDot (8pt colored circle for ok/stale/error/unauthenticated/disabled)
    - RelativeTimestampLabel (TimelineView-driven "Updated Xs ago" ticking every 1s; relativeString(from:to:) test seam)
    - ClockEnvironmentKey (SOLE declaration of ClockKey + EnvironmentValues.clockService, B5)
  affects:
    - Plan 01.08 (composition root — INJECTS \.clockService via .environment; hosts PopoverRootView in MenuBarExtra scene)
tech_stack:
  added:
    - "TimelineView(.periodic(from:by:)) for 1-second tick driving RelativeTimestampLabel (Phase Success Criterion #3)"
    - "SwiftUI Environment key pattern (ClockKey: EnvironmentKey) — sole declaration B5"
    - "GeometryReader for proportional QuotaBar fill width"
  patterns:
    - "@Environment(AggregateStore.self) consumption pattern (no @StateObject/ObservableObject)"
    - "@Environment(\\.clockService) injection seam (key declared once in ClockEnvironmentKey.swift, B5)"
    - "internal static pure-function test seam pattern (QuotaBar.color(forFraction:), RelativeTimestampLabel.relativeString(from:to:))"
    - "Source-grep @Test pattern for SwiftUI contract verification (FooterViewTests W7)"
key_files:
  created:
    - AgentsUsageBar/UI/Environment/ClockEnvironmentKey.swift
    - AgentsUsageBar/UI/Components/QuotaBar.swift
    - AgentsUsageBar/UI/Components/StatusDot.swift
    - AgentsUsageBar/UI/Components/RelativeTimestampLabel.swift
    - AgentsUsageBar/UI/TotalsHeaderView.swift
    - AgentsUsageBarTests/UITests/QuotaBarTests.swift
    - AgentsUsageBarTests/UITests/RelativeTimestampLabelTests.swift
    - AgentsUsageBarTests/UITests/FooterViewTests.swift
  modified:
    - AgentsUsageBar/UI/PopoverRootView.swift (replaced Wave 0 placeholder)
    - AgentsUsageBar/UI/ProviderRowView.swift (replaced Wave 0 placeholder)
    - AgentsUsageBar/UI/FooterView.swift (replaced Wave 0 placeholder)
    - AgentsUsageBar.xcodeproj/project.pbxproj (UI/Components + UI/Environment subgroups; UITests group)
key-decisions:
  - "ClockKey declared ONCE in ClockEnvironmentKey.swift (B5) — FooterView consumes only, Plan 01.08 injects only"
  - "QuotaBar color thresholds match UI-02 authoritative spec verbatim: nil→gray, <0.20→red, <0.50→yellow, ≥0.50→green (B4)"
  - "RelativeTimestampLabel uses days (not absolute date string) for >24h elapsed — compact, avoids locale complexity (Phase 1 trade-off)"
  - "TotalsHeaderView created in Task 1 commit to satisfy build (registered in pbxproj alongside components)"
  - "doc-comment mentions of forbidden patterns (@StateObject etc.) are intentional anti-pattern reminders — no actual code violations"
requirements-completed: [UI-01, UI-02, UI-06, UI-07, UI-10]
duration: 7min
completed: "2026-05-12"
---

# Phase 01 Plan 06: Popover UI Summary

**SwiftUI popover with `TotalsHeaderView` + color-coded `QuotaBar` (UI-02 green/yellow/red/gray per B4) + `RelativeTimestampLabel` (TimelineView 1s tick) + `FooterView` (Cmd-R refresh via `\.clockService`) — 20 new tests, sole `ClockKey` declaration (B5), no loading flash (UI-07)**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-12T04:28:49Z
- **Completed:** 2026-05-12T04:35:56Z
- **Tasks:** 2 (Task 1: components + tests; Task 2: popover scene views)
- **Files created:** 8
- **Files modified:** 4 (3 Wave 0 view replacements + xcodeproj)

## What Was Built

### Task 1: ClockEnvironmentKey + components + boundary tests (commit `ae7ec37`)

| File | Lines | Key export |
|------|-------|-----------|
| `ClockEnvironmentKey.swift` | 18 | Sole `ClockKey: EnvironmentKey` + `EnvironmentValues.clockService` (B5) |
| `QuotaBar.swift` | 95 | Color-coded bar with `internal static color(forFraction:)` test seam (B4) |
| `StatusDot.swift` | 75 | 8pt circle for ok/stale/error/unauthenticated/disabled |
| `RelativeTimestampLabel.swift` | 60 | `TimelineView(.periodic)` 1s tick; `relativeString(from:to:)` test seam |
| `TotalsHeaderView.swift` | 52 | Today total tokens + USD from `store.totals` (UI-01) |
| `QuotaBarTests.swift` | 55 | 8 `@Test` B4 boundary cases — all pass |
| `RelativeTimestampLabelTests.swift` | 55 | 9 `@Test` W2 boundary cases — all pass |
| `FooterViewTests.swift` | 55 | 3 `@Test` W7 source-grep contract tests — all pass |

**QuotaBarTests (8 assertions) — B4 boundaries verified:**

| Test | Expected |
|------|----------|
| nil fraction | Color.gray |
| 0.0 | Color.red |
| 0.19 | Color.red |
| 0.20 | Color.yellow (inclusive lower) |
| 0.49 | Color.yellow |
| 0.50 | Color.green (inclusive lower) |
| 0.99 | Color.green |
| 1.0 | Color.green (overage clamp) |

**RelativeTimestampLabelTests (9 assertions) — W2 boundaries verified:**

| Test | Expected |
|------|----------|
| 0s | "0s" |
| 1s | "1s" |
| 59s | "59s" |
| 60s | "1m" |
| 3599s | "59m" |
| 3600s | "1h" |
| 86399s | "23h" |
| 86400s | "1d" |
| 90000s | "1d" (Phase 1: days, not absolute date) |

### Task 2: PopoverRootView + ProviderRowView + FooterView (commit `b341836`)

| File | Lines | Key export |
|------|-------|-----------|
| `PopoverRootView.swift` | 55 | Root view; `.frame(width:360)`; sorted providers; `.task` POLL-03 refresh |
| `ProviderRowView.swift` | 145 | Full row: StatusDot + tokens/USD/balance + QuotaBar + RelativeTimestampLabel + "Resets —" |
| `FooterView.swift` | 55 | Cmd-R → `store.refresh(now: clock.now())`; Cmd-Q → `NSApp.terminate(nil)` |

## B4 QuotaBar Color Threshold Verification (UI-02 Authoritative)

Thresholds in `QuotaBar.color(forFraction:)` match REQUIREMENTS.md UI-02 verbatim:

| fraction | Color | Meaning |
|----------|-------|---------|
| `nil` | `Color.gray` + "no limit" | No-limit account (D-14, ROUTER-03) |
| `< 0.20` | `Color.red` | Critical — 20% remaining |
| `< 0.50` | `Color.yellow` | Warning — 20–50% remaining |
| `≥ 0.50` | `Color.green` | Healthy — ≥ 50% remaining |

Source literals `< 0.20` and `< 0.50` verified present in `QuotaBar.swift` (3 occurrences each in body + function).

## B5 ClockKey Sole-Ownership Verification

- `struct ClockKey: EnvironmentKey` declared **exactly once**: `UI/Environment/ClockEnvironmentKey.swift:11`
- All other files: 0 occurrences (verified via `grep -rIEc 'struct ClockKey' AgentsUsageBar/`)
- `FooterView.swift` contains `@Environment(\.clockService)` (consumer) and 0 occurrences of `struct ClockKey` or `extension EnvironmentValues`

## Test Suite Breakdown

| Suite | Assertions | Status |
|-------|-----------|--------|
| AgentsUsageBarSkeletonTests | 3 | ✓ all pass |
| SecretTests | 4 | ✓ all pass |
| TodayHelperTests | 6 | ✓ all pass |
| QuotaTests | 4 | ✓ all pass |
| ThresholdStateTests | 9 | ✓ all pass |
| FileCacheStoreTests | 9 | ✓ all pass |
| URLSessionHTTPClientTests | 8 | ✓ all pass |
| TomlReaderTests | 10 | ✓ all pass |
| ConfigStoreTests | 14 | ✓ all pass |
| ResponseDecodingTests | 6 | ✓ all pass |
| OpenRouterProviderTests | 5 | ✓ all pass |
| BaselineDeltaTests | 8 | ✓ all pass |
| AggregateStoreTests | 10 | ✓ all pass |
| PollSchedulerTests | 6 | ✓ all pass |
| **QuotaBarTests (new)** | **8** | **✓ all pass** |
| **RelativeTimestampLabelTests (new)** | **9** | **✓ all pass** |
| **FooterViewTests (new)** | **3** | **✓ all pass** |
| **Total** | **122** | **✓ all pass** |

(126 test-case lines in xcodebuild output due to parallel test runner reporting some cases twice)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] TotalsHeaderView.swift created in Task 1 to satisfy build**
- **Found during:** Task 1 first build after pbxproj registration
- **Issue:** `TotalsHeaderView.swift` was registered in pbxproj as part of Task 1's UI group additions, but the file was a Task 2 deliverable — causing `BUILD FAILED: Build input file cannot be found`
- **Fix:** Created `TotalsHeaderView.swift` as part of Task 1 commit (its content was already fully specified in the plan; no scope difference)
- **Files modified:** `AgentsUsageBar/UI/TotalsHeaderView.swift`
- **Commit:** `ae7ec37`

**2. [Rule 1 - Bug] StatusDot previews used incorrect ProviderError constructor**
- **Found during:** Task 1 source authoring
- **Issue:** Preview code used `.networkError("...")` which doesn't exist — `ProviderError` is a struct with `kind: Kind` + `message: String` fields, not an enum with associated values
- **Fix:** Changed to `ProviderError(kind: .network, message: "...")` before first build attempt
- **Files modified:** `AgentsUsageBar/UI/Components/StatusDot.swift`
- **Commit:** `ae7ec37`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both minor — TotalsHeaderView was created one task early (content identical to plan), ProviderError preview constructor corrected before build. No scope creep.

## Known Stubs

None — all UI views render real data from `AggregateStore`. The "Resets —" value in `ProviderRowView` is intentional (OpenRouter has no per-day reset countdown — RESEARCH Open Question #4). This is correct behavior for Phase 1, not a stub.

## Threat Surface Scan

No new threat surfaces beyond the plan's `<threat_model>`. Views are pure SwiftUI render — no network calls, no file access, no new trust boundaries. The T-01-06-01 (info disclosure) mitigation is satisfied: no `UsageSnapshot.raw` or credential-derived values are rendered.

## Self-Check: PASSED

- [x] `AgentsUsageBar/UI/Environment/ClockEnvironmentKey.swift` exists with `struct ClockKey: EnvironmentKey`, `static let defaultValue: any Clock = SystemClock()`, `var clockService: any Clock`
- [x] `AgentsUsageBar/UI/Components/QuotaBar.swift` contains `public struct QuotaBar: View`, `public init(quota: Quota?)`, `internal static func color(forFraction fraction: Double?) -> Color`, literals `< 0.20` and `< 0.50`, `"no limit"`, `Color.red`, `Color.yellow`, `Color.green`, `Color.gray`
- [x] `AgentsUsageBar/UI/Components/StatusDot.swift` contains `public struct StatusDot: View`, `public init(status: ProviderStatus)`
- [x] `AgentsUsageBar/UI/Components/RelativeTimestampLabel.swift` contains `TimelineView(.periodic(from:` and `internal static func relativeString(from`
- [x] All three component files contain `#Preview` blocks (4/5/3 respectively)
- [x] `AgentsUsageBar/UI/PopoverRootView.swift` contains `public struct PopoverRootView: View`, `@Environment(AggregateStore.self)`, `.frame(width: 360)`, `.task {`, `store.refresh(now:`
- [x] `AgentsUsageBar/UI/FooterView.swift` contains `.keyboardShortcut("r", modifiers: .command)`, `.keyboardShortcut("q", modifiers: .command)`, `NSApp.terminate(nil)`, `store.refresh(now:`, `@Environment(\.clockService)` — and does NOT contain `struct ClockKey` or `extension EnvironmentValues`
- [x] `struct ClockKey` appears exactly once in the codebase (ClockEnvironmentKey.swift:11)
- [x] No `@StateObject`, `@ObservedObject`, `ObservableObject`, `@Published`, or `import Combine` in any new production file
- [x] `QuotaBarTests.swift` has 8 `@Test` annotations — all pass
- [x] `RelativeTimestampLabelTests.swift` has 9 `@Test` annotations — all pass
- [x] `FooterViewTests.swift` has 3 `@Test` annotations — all pass
- [x] Commit `ae7ec37` exists (Task 1)
- [x] Commit `b341836` exists (Task 2)
- [x] `xcodebuild build` exits 0 — BUILD SUCCEEDED
- [x] `xcodebuild test` QuotaBarTests — TEST SUCCEEDED
- [x] `xcodebuild test` RelativeTimestampLabelTests — TEST SUCCEEDED
- [x] `xcodebuild test` FooterViewTests — TEST SUCCEEDED
- [x] `xcodebuild test` (full suite) — TEST SUCCEEDED (122 assertions)
