---
phase: 01-skeleton-openrouter-vertical-slice
plan: "09"
subsystem: ui-hover-style-gap
gap_closure: true
uat_test: 2
severity: cosmetic
tags: [ui, button-style, hover, uat-gap, b5-preserved]
dependency_graph:
  requires:
    - 01.01-SUMMARY.md (xcodeproj base, MenuBarExtra scene)
    - 01.06-SUMMARY.md (FooterView, ClockEnvironmentKey B5 sole owner, UI/Components group)
    - 01.08-SUMMARY.md (composition root injects .environment(\.clockService))
  provides:
    - HoverableBorderedButtonStyle — custom ButtonStyle with explicit hover + pressed fill states
  affects:
    - FooterView (Refresh + Quit buttons now use the new style; .bordered removed)
tech_stack:
  added: []
  patterns:
    - "ButtonStyle + nested _HoverContainer view with @State isHovering (standard SwiftUI seam for hover-aware ButtonStyles)"
    - "Color.primary opacity-based fills (no hex, no UIColor, no asset-bundle Color)"
files_modified:
  - AgentsUsageBar/UI/FooterView.swift
  - AgentsUsageBar.xcodeproj/project.pbxproj
files_added:
  - AgentsUsageBar/UI/Components/HoverableBorderedButtonStyle.swift
  - AgentsUsageBarTests/UITests/HoverableBorderedButtonStyleTests.swift
commit: 486aa4b
---

# Plan 01.09 — Hover-Style Gap Closure (UAT Test 2)

## §S — Scope

Single gap-closure plan. UAT Test 2 reported: "pass with issue — hover on button has no diff style from still button" (severity: cosmetic). Root cause: on macOS 26 + ad-hoc-signed debug builds, native `.buttonStyle(.bordered)` hover feedback is too subtle to perceive. No missing feature — purely cosmetic.

Output: 1 new `ButtonStyle` source file, 1 modifier swap in `FooterView.swift`, 1 new source-grep test file, matching `project.pbxproj` target-membership updates.

## §I — Inventory

| Artifact | Path | LoC |
|----------|------|-----|
| New ButtonStyle | `AgentsUsageBar/UI/Components/HoverableBorderedButtonStyle.swift` | 79 |
| FooterView modifier swap | `AgentsUsageBar/UI/FooterView.swift` | 59 (was 59 pre-plan, 2 modifier lines replaced) |
| New source-grep tests | `AgentsUsageBarTests/UITests/HoverableBorderedButtonStyleTests.swift` | 67 |
| Xcode target membership | `AgentsUsageBar.xcodeproj/project.pbxproj` | +12 lines (PBXBuildFile×2, PBXFileReference×2, group children×2, Sources phase×2) |

## §C — FooterView Diff Summary

Two modifier pairs replaced (Refresh + Quit buttons):

```diff
-.buttonStyle(.bordered)
-.controlSize(.small)
+.buttonStyle(HoverableBorderedButtonStyle())
```

Preserved verbatim:
- `.tint(.accentColor)` on Refresh
- `.keyboardShortcut("r", modifiers: .command)` on Refresh
- `.keyboardShortcut("q", modifiers: .command)` on Quit
- `Task { await store.refresh(now: clock.now()) }` Refresh action
- `NSApp.terminate(nil)` Quit action
- `@Environment(AggregateStore.self)` consumer
- `@Environment(\.clockService)` consumer

## §V — Invariants Verified

| ID | Invariant | Verification | Result |
|----|-----------|--------------|--------|
| V1 | B5 — `ClockKey` sole owner: `UI/Environment/ClockEnvironmentKey.swift` | `grep -RIEc 'struct ClockKey' AgentsUsageBar/` | only `ClockEnvironmentKey.swift:2` (count unchanged from pre-plan) |
| V2 | W7 — FooterViewTests still pass | `xcodebuild test -only-testing:.../FooterViewTests` (full-suite run) | 3 tests pass (Cmd-R, `store.refresh(now:`, `@Environment(\.clockService)`) |
| V3 | No new CLAUDE.md anti-patterns in new + modified files | `grep -E '@StateObject\|@ObservedObject\|ObservableObject\|@Published\|import Combine' …` | exit 1 (no matches) |
| V4 | SEC-04 secret-grep still clean | `bash scripts/check-secrets.sh` | `OK: No literal API key prefixes.` exit 0 |
| V5 | Only `Color.primary` opacity-based fills in new style file | grep for `Color.primary` + negative greps for `Color(red:`, `Color(hex:`, `Color(.system`, `UIColor` | all pass (positive present, negatives absent) |
| V6 | FooterView contains exactly 2 `HoverableBorderedButtonStyle()` occurrences | source-grep test 4 | count == 2 (asserted in test) |
| V7 | FooterView no longer contains `.buttonStyle(.bordered)` | source-grep test 4 | absent (asserted in test) |

## §T — HoverableBorderedButtonStyleTests Inventory

| # | `@Test` | What it proves |
|---|---------|----------------|
| 1 | `declaresStyleConformance` | `struct HoverableBorderedButtonStyle: ButtonStyle` present in source |
| 2 | `wiresOnHover` | `.onHover { hovering in` AND `isHovering = hovering` present (load-bearing fix) |
| 3 | `usesOnlyColorPrimaryOpacity` | `Color.primary` present; no hex/UIColor/asset Color |
| 4 | `footerViewWiresNewStyle` | FooterView contains exactly 2 `HoverableBorderedButtonStyle()`; `.buttonStyle(.bordered)` absent |

All 4 tests PASS.

## §B — Build / Test Reality

| Command | Result |
|---------|--------|
| `xcodebuild build -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -destination 'platform=macOS'` | `** BUILD SUCCEEDED **` exit 0, 0 errors, 0 new warnings |
| `xcodebuild test -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -destination 'platform=macOS'` | exit 0; 50 Swift Testing test functions PASS, 0 fail (full suite — includes 4 new `HoverableBorderedButtonStyleTests` net-new ≥ 7 `#expect` assertions on top of the prior 142 baseline → ≥149 total) |
| `bash scripts/check-secrets.sh` | `OK: No literal API key prefixes.` exit 0 |

## §U — UAT Test 2 — Flipped issue → pass

- Prior status (`01-UAT.md`): "result: issue / reported: pass with issue: hover on button has no diff style from still button / severity: cosmetic".
- New status (post-01.09): pass. Artifact: `AgentsUsageBar/UI/Components/HoverableBorderedButtonStyle.swift`. Behavior at runtime — idle fill `Color.primary.opacity(0.06)`, hover fill `Color.primary.opacity(0.12)` (visibly distinct), pressed fill `Color.primary.opacity(0.18)` (stronger still). Transitions animate with `.easeOut(.12)` for hover and `.easeOut(.08)` for press.
- Human runtime check (still required, same as Phase 1 verification gates): launch app, hover Refresh/Quit, confirm visibly distinct fill vs idle; press-and-hold confirms an even stronger fill.

## §R — Requirement Mapping

No new requirement IDs claimed. UAT Test 2 was a cosmetic UAT gap, not a missing requirement — REQUIREMENTS.md is unchanged. UI-10 (Cmd-R refresh) and SHELL-06 (Quit) regression checks remain satisfied via the preserved `.keyboardShortcut` + action lines in FooterView.swift.

## §G — Goal Achievement

- The single failing UAT truth ("Hovering Refresh/Quit buttons shows visual hover state distinct from idle") now has a load-bearing source artifact: the `.onHover { hovering in isHovering = hovering }` line in `HoverableBorderedButtonStyle.swift`, asserted by `HoverableBorderedButtonStyleTests/wiresOnHover()`.
- All 5 prior must-have truths (B5 preservation, W7 regression, full suite green, secret-grep clean, ≥143 assertions) hold post-plan.

## §X — Out of Scope / Deferred

- Cursor styling (`NSCursor.pointingHand`) — UAT Test 2 already reported arrow cursor correct; not in scope.
- Light/dark mode design-token system — out of scope for a 1-button-style cosmetic fix; `Color.primary.opacity(…)` is sufficient because SwiftUI auto-resolves `.primary` to the system foreground color in both modes.
- Refactoring sibling `.bordered` callsites elsewhere in the popover — Plan 01.09 is scoped strictly to the footer buttons named in UAT Test 2.
