---
phase: 02-claude-provider-threshold-rollover-jsonl-streaming
plan: "02"
subsystem: claude-pricing
requirements: [CLAUDE-05]
salvage: true
dependency_graph:
  requires:
    - Domain Decimal arithmetic convention (D-28 precedent)
  provides:
    - ClaudeModelPricing — bundled USD pricing table + lookup cascade
    - cost calculator returning Decimal — consumed by Plan 02.04 ClaudeProvider snapshot building
  affects: []
tech_stack:
  added: []
  patterns:
    - "Lookup cascade: exact id → prefix → contains-family → file default (RESEARCH §C.1)"
    - "cache_read = 0.10 × input ∀ Claude model (Anthropic 2026 pricing — verified)"
    - "Decimal arithmetic ∴ no IEEE 754 drift on small monetary values (D-28)"
    - "Bundle.main.url(forResource:withExtension:) for shipped JSON resource"
files_added:
  - AgentsUsageBar/Providers/Claude/ClaudeModelPricing.swift
  - AgentsUsageBar/Resources/Pricing/claude-models.json (bundled in app target Resources Build Phase)
  - AgentsUsageBarTests/ProvidersClaudeTests/ClaudeModelPricingTests.swift
  - AgentsUsageBarTests/ProvidersClaudeTests/Fixtures/pricing-test-models.json
files_modified:
  - AgentsUsageBar.xcodeproj/project.pbxproj (Pricing resource subgroup + target memberships)
commit: 2bf5bf6
salvage_notes: |
  02-02 was the only Wave-1 agent that signaled completion (the other two
  were killed). It wrote into the same shared main checkout as 02-01 / 02-03,
  but its files don't overlap with theirs and survived intact. Test fixes
  applied during salvage:
    - `loadBundled_throwsBundleResourceMissing_inTestContext` → inverted to
      `loadBundled_succeedsInAppHostedTestTarget`. Bundle.main during
      app-hosted xctest is the AgentsUsageBar.app bundle (which ships
      claude-models.json), so the production loadBundled() path succeeds.
    - `bundledJSON_cacheReadRatio_holdsForAllShippedModels` path navigation
      fixed: removed one .deletingLastPathComponent() (test file lives
      directly in ProvidersClaudeTests, not inside Fixtures).
---

# Plan 02.02 — Bundled Claude Pricing Table + Cost Calc

## §S — Scope

CLAUDE-05: ship a hand-curated USD pricing subset for all Claude models we care
about, with a 4-level lookup cascade and a Decimal-arithmetic cost calculator.

No build-time fetch of `BerriAI/litellm` — CI brittleness. JSON ships in the
app bundle and is read once at startup.

## §I — Inventory

| Artifact | Path |
|----------|------|
| Pricing model + cascade + cost calc | `AgentsUsageBar/Providers/Claude/ClaudeModelPricing.swift` |
| Production pricing table | `AgentsUsageBar/Resources/Pricing/claude-models.json` |
| Tests | `AgentsUsageBarTests/ProvidersClaudeTests/ClaudeModelPricingTests.swift` |
| Test fixture | `AgentsUsageBarTests/ProvidersClaudeTests/Fixtures/pricing-test-models.json` |

## §V — Invariants Verified

| ID | Invariant | Verification | Result |
|----|-----------|--------------|--------|
| V1 | Exact id match wins | `rate_exactMatch_returnsExactRate` | ✅ |
| V2 | Prefix match when model id is longer than registered key | `rate_prefixMatch_modelIDLonger` | ✅ |
| V3 | nil model → default rate | `rate_nilModel_returnsDefault` | ✅ |
| V4 | cache_read = 0.10 × input across all shipped models (CLAUDE-05) | `bundledJSON_cacheReadRatio_holdsForAllShippedModels` | ✅ |
| V5 | Bundle.main loads claude-models.json in app-hosted test | `loadBundled_succeedsInAppHostedTestTarget` | ✅ |

## §T — Tests Inventory

`ClaudeModelPricingTests` — cascade tests + cache-read-ratio invariant +
loadBundled smoke. All green.

## §B — Backprop Notes

- B1: `loadBundled_throwsBundleResourceMissing_inTestContext` was authored
  assuming Bundle.main during xctest is the test runner. For an app-hosted
  test target (test host = AgentsUsageBar.app), Bundle.main is the host
  app — which ships the resource. Test was inverted to assert the success
  path; an explicit-bundle negative test can be added in Plan 02.04 if needed.
- B2: `#filePath` is the literal source file path. Navigation should match the
  file's actual location, not a hypothetical "Fixtures" position the comment
  drafts assumed.
