---
plan_id: 04-07
phase: 04
title: UI — ProviderRowView secondary-line branching for isLocal rows + LocalRowSecondaryView helper + tooltip wiring + placeholderMessage rendering
wave: 3
depends_on: [04-04, 04-05, 04-06]
files_modified:
  - AgentsUsageBar/UI/ProviderRowView.swift
  - AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift
  - AgentsUsageBarTests/UITests/ProviderRowViewLocalRowTests.swift
  - AgentsUsageBarTests/UITests/LocalRowSecondaryViewTests.swift
  - AgentsUsageBar.xcodeproj/project.pbxproj
requirements: [LOCAL-04, LOCAL-06]
autonomous: true
estimated_duration_min: 60
---

## Objective

Branch `UI/ProviderRowView.swift` to render the **five D-02/D-03 row states** for `isLocal=true` rows, replacing the existing `tokens · USD · balance` HStack (lines 72-91 — meaningless for locals per LOCAL-06) with a new `LocalRowSecondaryView` helper. The helper consumes the snapshot.raw fields stamped by Plans 04-04 / 04-05 / 04-06 (`modelName`, `modelCount`, `vramBytes`, `allModels`, `loadingModel`) and renders the appropriate string:

| State | Trigger | Rendered |
|-------|---------|----------|
| A | lastStatus == .notRunning | `"Not running"` |
| B | modelCount == "0" + lastStatus == .ok | `"Idle — 0 models loaded"` |
| B' (degenerate, may collapse to B) | modelCount == "0" + installedCount == "0" | `"Idle — no models installed"` |
| C | modelCount == "1" | `"<modelName>"` optionally `· X.X GB VRAM` |
| D | modelCount > 1 | `"<firstModelName> · +N more"` |
| E | loadingModel == "true" | `"Running — loading model…"` |

Additionally, the row renders `state.placeholderMessage` as the secondary line when set (the D-04 unconfigured-llama.cpp discoverability subtitle — Plan 04-08 seeds this via the extended `seedPlaceholder` from Plan 04-06).

Tooltip channel (`.help(snapshot.tooltipLabel ?? "")`) is reused unchanged from Phase 3 STATE #88 / Plan 03-07 — local actors stamp `tooltipLabel` with model family / quantization summary; SwiftUI suppresses `.help` when the argument is empty.

`StatusDot` ripple landed in Plan 04-01 (`case .notRunning → .gray`).

## Context

Plan 03-07 / STATE #88-92 wired the rich `ProviderRowView` for Phase 3 (D-15 tooltips + UI-11 dashboard button + D-11 degraded styling). The view body keys off `state.snapshot?.raw` extensively. Plan 04-07 adds **one** new branch: if the row's provider id is in `ProviderID.localIDs` (Plan 04-01 declared this Set), render the `LocalRowSecondaryView` in place of the tokens/USD/balance HStack.

Capability lookup strategy (RESEARCH §5.2): the cheapest seam is `ProviderID.localIDs: Set<ProviderID>` (Plan 04-01 declared). The view doesn't need the `ProviderCapabilities` instance; it just checks `if ProviderID.localIDs.contains(state.id)`.

LOCAL-06 enforcement at the UI layer: the new view code path must NEVER reach `tokenText(_:)`, `usdText(_:)`, `balanceText(_:)` for local rows. Plan 04-07's negative grep includes a test asserting that local rows render no `tokens` / `USD` / `bal` strings.

Files referenced:
- `AgentsUsageBar/UI/ProviderRowView.swift:51-149` — view body; lines 72-91 are the HStack being branched.
- `AgentsUsageBar/UI/ProviderRowView.swift:154-200` — private helpers (`tokenText`, `usdText`, `balanceText`, `resetsText`) — preserved untouched for non-local rows.
- `AgentsUsageBar/Domain/ProviderID.swift` (post-04-01) — `static let localIDs: Set<ProviderID>` carrier.
- `AgentsUsageBar/Domain/ProviderState.swift:28` — `placeholderMessage: String?` — render when non-nil for any provider (including local placeholder rows seeded by Plan 04-08).

Existing Plan 03-07 invariants preserved:
- StatusDot.forceAmber for D-11 degraded
- `.opacity(isStale || isDegraded ? 0.6 : 1.0)` composition
- Trailing dashboard button always-visible + `.disabled(dashboardURL == nil)` for local IDs (per Phase 3 STATE #89 — `ProviderDashboardURL.lookup(.ollama)` already returns nil)
- TimelineView(.periodic(by: 1)) UI-08 stale dimming

## Tasks

<task id="T-04-07-01">
  <title>Create `LocalRowSecondaryView` SwiftUI helper rendering the five row states + placeholderMessage</title>
  <type>execute</type>
  <read_first>
    - AgentsUsageBar/UI/ProviderRowView.swift (entire — view body shape)
    - AgentsUsageBar/UI/Components/StatusDot.swift (post-04-01 — additive parameter pattern)
    - 04-RESEARCH.md §5.1 — Row state table A..E
    - 04-RESEARCH.md §5.2 — view-side branching pseudocode
    - 04-CONTEXT.md D-02 + D-02a + D-03 + D-04
  </read_first>
  <action>
    Create `AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift`:

    ```
    import SwiftUI

    /// Plan 04-07 — secondary-line rendering for local (Ollama / LM Studio / llama.cpp) rows.
    ///
    /// Replaces the tokens · USD · balance HStack from ProviderRowView (Phase 1 lines 72-91)
    /// for any row whose ProviderID is in ProviderID.localIDs (per Plan 04-01 STATE).
    ///
    /// LOCAL-06 anti-feature enforcement: this view NEVER renders token / cost / balance
    /// figures. The verification gate is the test suite LocalRowSecondaryViewTests + a
    /// ProviderRowView source-grep asserting locals NEVER reach tokenText/usdText/balanceText.
    public struct LocalRowSecondaryView: View {
        public let state: ProviderState
        public init(state: ProviderState) { self.state = state }

        public var body: some View {
            Text(secondaryText)
                .font(.caption2)
                .foregroundStyle(.secondary)
        }

        /// Pure value-type helper — exposed `internal` for direct unit-testing.
        internal var secondaryText: String {
            // D-04 placeholder takes precedence — Plan 04-08 seeds this with the
            // "Set [llamacpp] port in config.toml to enable" subtitle.
            if let msg = state.placeholderMessage, !msg.isEmpty {
                return msg
            }

            // State E (RESEARCH §5.1 row E) — llama.cpp transient warmup.
            if state.snapshot?.raw["loadingModel"] == "true" {
                return "Running — loading model…"
            }

            // State A — lastStatus == .notRunning (RESEARCH §5.1 row A).
            if case .notRunning = state.status {
                return "Not running"
            }

            let raw = state.snapshot?.raw ?? [:]
            let count = Int(raw["modelCount"] ?? "") ?? 0
            let installedCount = Int(raw["installedCount"] ?? "") ?? -1
            let name = raw["modelName"]
            let vramBytes = Int64(raw["vramBytes"] ?? "") ?? 0
            let vramSuffix: String = {
                guard vramBytes > 0 else { return "" }
                // RESEARCH §5.1 — format "X.X GB VRAM" via Double / GiB.
                let gb = Double(vramBytes) / 1_073_741_824.0
                return String(format: " · %.1f GB VRAM", gb)
            }()

            if count == 0 {
                if installedCount == 0 {
                    return "Idle — no models installed"  // State B' (degenerate)
                }
                return "Idle — 0 models loaded"  // State B
            }

            if count == 1, let name {
                return "\(name)\(vramSuffix)"  // State C
            }

            if count > 1, let name {
                return "\(name) · +\(count - 1) more"  // State D
            }

            // Fallback — should never reach unless rendering a snapshot from a
            // future provider with an unknown raw shape; print "—" as a safe default.
            return "—"
        }
    }
    ```

    The `internal var secondaryText: String` accessor is the seam for `LocalRowSecondaryViewTests` to assert each branch without instantiating SwiftUI views.

    Add SwiftUI `#Preview` blocks for all five states (A, B, B', C, D, E + placeholderMessage) — mirrors Plan 03-07 `#Preview` count for ProviderRowView.

    **LOCAL-06 invariant**: this file contains zero references to `tokens`, `USD`, `bal`, `costToday`, `tokensToday`. Verified by negative grep in acceptance.
  </action>
  <acceptance_criteria>
    - `grep 'public struct LocalRowSecondaryView' AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift` returns 1.
    - `grep 'internal var secondaryText: String' AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift` returns 1 (test seam).
    - `grep -E 'Not running|Idle — 0 models loaded|Idle — no models installed|Running — loading model' AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift` returns ≥ 4 (literal strings for states A, B, B', E — locked).
    - **Negative invariant — LOCAL-06**: `grep -E 'tokens|USD|bal\b|costToday|tokensToday|formattingCurrency' AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift` returns 0.
    - **Negative invariant — no AppKit**: `grep 'NSWorkspace\|NSAlert' AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift` returns 0 (pure SwiftUI).
  </acceptance_criteria>
</task>

<task id="T-04-07-02">
  <title>Branch `ProviderRowView` body — use `LocalRowSecondaryView` when `ProviderID.localIDs.contains(state.id)`</title>
  <type>execute</type>
  <read_first>
    - AgentsUsageBar/UI/ProviderRowView.swift (entire — body lines 51-149)
    - AgentsUsageBar/Domain/ProviderID.swift (post-04-01 — `localIDs` Set)
    - Phase 3 STATE #88-92 — Plan 03-07 ProviderRowView modifications (StatusDot.forceAmber + tooltip + dashboard button preserved)
    - 04-RESEARCH.md §5.2 — view-side branching specification
    - 04-RESEARCH.md §5.3 — isDegraded / isStale composition
  </read_first>
  <action>
    Edit `AgentsUsageBar/UI/ProviderRowView.swift`:

    1. Add a computed property: `private var isLocal: Bool { ProviderID.localIDs.contains(state.id) }`.

    2. In the view body (line 51-149), inside the `VStack(alignment: .leading, spacing: 4)` (line 63), replace the existing `HStack(spacing: 4) { ... }` block (lines 72-91 — tokens · USD · balance) with a conditional:

       ```
       if isLocal {
           LocalRowSecondaryView(state: state)
               .opacity(isStale || isDegraded ? 0.6 : 1.0)
       } else {
           HStack(spacing: 4) {
               Text(tokenText(state.snapshot))
                   .font(.caption2)
                   .foregroundStyle(.secondary)
               // ... existing Phase 1/3 HStack contents unchanged ...
           }
           .opacity(isStale || isDegraded ? 0.6 : 1.0)
       }
       ```

       The `.opacity(isStale || isDegraded ? 0.6 : 1.0)` modifier composes the UI-08 stale dim + the D-11 degraded dim per Phase 3 STATE #88 — preserve verbatim.

    3. The quota bar (line 95-98 `QuotaBar(quota: state.snapshot?.quota)`) renders for ALL rows; for locals `state.snapshot?.quota == nil` so `QuotaBar` renders empty per Phase 1 D-14 / ROUTER-03 precedent. No change needed.

    4. The "Resets — Xh Ym" countdown row (line 108-115 from Plan 03-07 hotfix G-01) renders for ALL rows; locals have `quotaWindows == nil` per Plan 04-04/05/06 so `resetsText` returns `"Resets —"` — matches the documented OpenRouter/Claude behavior. No change needed.

    5. The D-11 degraded subtitle (line 120-124) doesn't fire for locals because locals don't stamp `raw["note"] == ThresholdEngine.degradedTag` (Phase 3 D-11 is Gemini-specific). However the local actor's `degradedSnapshot` may set `raw["note"]` — IMPORTANT design rule: Plan 04-04/05/06 actors must NOT set `raw["note"] = ThresholdEngine.degradedTag` because:
       (a) Locals have no quota so threshold notifications never fire anyway (RESEARCH §10 row 6).
       (b) The D-11 "usage temporarily unavailable" subtitle is misleading for a local provider.
       Verify this by adding to plan 04-07's acceptance: `grep 'degradedTag' AgentsUsageBar/Providers/{Ollama,LMStudio,LlamaCpp}/*.swift` returns 0.

    6. Trailing dashboard button (line 127-144 from Plan 03-07) — unchanged. `ProviderDashboardURL.lookup(.ollama)` returns nil → `.disabled(dashboardURL == nil)` keeps the button visible-but-disabled per Phase 3 STATE #89. Symmetry preserved.

    7. Add a `#Preview` block: `#Preview("ProviderRowView — Ollama running single model") { ... }` showing a typical Ollama row with state.id == .ollama, snapshot with `raw["modelName"] == "llama3:8b"`, `raw["modelCount"] == "1"`, `raw["vramBytes"] == "5137025024"`, status == .ok. Add another `#Preview` for the .notRunning state.

    Do NOT touch the existing private helpers (`tokenText`, `usdText`, `balanceText`, `resetsText`) — they remain in place for non-local rows. Plan 04-07 is a strictly additive branch.
  </action>
  <acceptance_criteria>
    - `grep 'private var isLocal: Bool { ProviderID.localIDs.contains' AgentsUsageBar/UI/ProviderRowView.swift` returns 1.
    - `grep 'LocalRowSecondaryView' AgentsUsageBar/UI/ProviderRowView.swift` returns ≥ 1.
    - `grep 'if isLocal {' AgentsUsageBar/UI/ProviderRowView.swift` returns 1.
    - **Negative invariant — local actors do NOT stamp degradedTag**: `grep 'degradedTag\|usage-temporarily-unavailable' AgentsUsageBar/Providers/Ollama AgentsUsageBar/Providers/LMStudio AgentsUsageBar/Providers/LlamaCpp` returns 0.
    - **Preserved invariant — Phase 3 dashboard button still visible**: `grep 'arrow.up.right.square' AgentsUsageBar/UI/ProviderRowView.swift` returns 1 (unchanged from Plan 03-07).
    - **Preserved invariant — Phase 3 D-11 degraded subtitle still emits for Gemini**: `grep 'Updated.*usage temporarily unavailable' AgentsUsageBar/UI/ProviderRowView.swift` returns 1 (unchanged from Plan 03-07).
    - `xcodebuild build -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -configuration Debug` exits 0 — view body still type-checks.
  </acceptance_criteria>
</task>

<task id="T-04-07-03">
  <title>Write `LocalRowSecondaryViewTests` + `ProviderRowViewLocalRowTests` Swift Testing suites</title>
  <type>execute</type>
  <read_first>
    - AgentsUsageBarTests/UITests/ProviderRowViewDegradedTests.swift (Phase 3 Plan 03-07 STATE #91 — source-grep + secondary-text tests)
    - AgentsUsageBarTests/UITests/ProviderRowViewDashboardButtonTests.swift (Phase 3 STATE #89)
    - 04-RESEARCH.md §5.1 — row state table (test rows for each)
  </read_first>
  <action>
    Create `AgentsUsageBarTests/UITests/LocalRowSecondaryViewTests.swift` — pure value-type tests on the `secondaryText` accessor (no SwiftUI runtime needed):

    Helper:
    ```
    private func makeState(
        id: ProviderID = .ollama,
        status: ProviderStatus = .ok(lastSuccess: .now),
        placeholderMessage: String? = nil,
        raw: [String: String] = [:]
    ) -> ProviderState {
        let snapshot = UsageSnapshot(
            providerID: id, asOf: .now,
            tokensToday: nil, costTodayUSD: nil, balanceUSD: nil,
            quota: nil, raw: raw
        )
        return ProviderState(
            id: id, displayName: id.displayHint,
            placeholderMessage: placeholderMessage, snapshot: snapshot,
            status: status, lastSuccess: .now
        )
    }
    ```

    Test cases (one per row state):
    - `stateA_notRunning_yieldsNotRunningString` — `#expect(LocalRowSecondaryView(state: makeState(status: .notRunning, raw: ["modelCount":"0"])).secondaryText == "Not running")`.
    - `stateB_idleZeroModelsLoaded_okStatusEmptyModelCount` — raw["modelCount"]="0", installedCount nil-or-positive → `"Idle — 0 models loaded"`.
    - `stateBPrime_idleNoModelsInstalled` — raw["modelCount"]="0", raw["installedCount"]="0" → `"Idle — no models installed"`.
    - `stateC_singleModelByName` — raw["modelCount"]="1", raw["modelName"]="llama3:8b" → `"llama3:8b"` (no VRAM suffix when vramBytes absent).
    - `stateC_singleModelWithVRAM` — raw["modelCount"]="1", raw["modelName"]="llama3:8b", raw["vramBytes"]="5137025024" → `"llama3:8b · 4.8 GB VRAM"` (rounded to .1 GB).
    - `stateC_singleModelZeroVRAM_suppressedSuffix` — raw["vramBytes"]="0" → no `" · 0.0 GB VRAM"` suffix (RESEARCH §5.1 + OQ-4 — CPU-only case).
    - `stateD_threeModelsLoaded` — raw["modelCount"]="3", raw["modelName"]="llama3:8b" → `"llama3:8b · +2 more"`.
    - `stateD_twoModelsLoaded` — count=2 → `"<name> · +1 more"`.
    - `stateE_loadingModel_yieldsLoadingString` — raw["loadingModel"]="true" → `"Running — loading model…"`. Status MUST be `.ok` (state E is a sub-state) but the rendering keys on the raw field.
    - `stateE_takesPrecedenceOverModelCount` — raw["loadingModel"]="true" + modelCount="3" → still `"Running — loading model…"` (loadingModel branch is checked FIRST).
    - `placeholderMessage_takesPrecedenceOverAllStates` — state with placeholderMessage="Set [llamacpp] port in config.toml to enable" + raw["modelCount"]="0" → returns the placeholder message verbatim (D-04 highest priority).
    - `emptyPlaceholderMessage_doesNotShadow` — placeholderMessage="" → falls through to the model-count logic (defensive: empty strings don't accidentally show as the placeholder).
    - `unknownState_fallbackToDashEm` — empty raw + .unauthenticated status → "—" (defensive fallback per the helper's tail).
    - `localIDsCarrier_containsOllamaLMStudioLlamacpp` — `#expect(ProviderID.localIDs == Set([.ollama, .lmstudio, .llamacpp]))` (regression — Plan 04-01 contract still holds).
    - `noLocal06Leak_secondaryTextNeverIncludesUSD` — render state with arbitrary snapshot containing tokensToday=999 + costTodayUSD=99.99 in the snapshot fields. Assert `secondaryText` does NOT contain `"USD"`, `"$"`, `"tokens"`, `"bal"` (locking the LOCAL-06 invariant at the view layer).

    Create `AgentsUsageBarTests/UITests/ProviderRowViewLocalRowTests.swift` — source-grep + view-render tests on ProviderRowView itself (mirrors Plan 03-07 STATE #91 source-walk pattern):

    - `providerRowView_referencesLocalRowSecondaryView` — read ProviderRowView.swift source; assert `contains("LocalRowSecondaryView")` and `contains("isLocal")` and `contains("ProviderID.localIDs.contains")`.
    - `providerRowView_branchesOnIsLocal` — same source-walk; assert `contains("if isLocal {")` and that the branch happens BEFORE the dashboard button rendering (line ordering invariant).
    - `providerRowView_local06AntiFeature_localsNeverReachTokenText` — clever assertion: source-walk verifies the `tokenText(state.snapshot)` invocation is reachable ONLY inside an `else` branch of `if isLocal`. Implementation: `String(contentsOf:)`-based grep that captures the lines between `if isLocal {` and the matching closing brace; assert `tokenText(`, `usdText(`, `balanceText(` are NOT among them.
    - `providerRowView_dashboardButtonStillVisibleForLocals` — Plan 03-07 invariant preserved: source contains `arrow.up.right.square` exactly once; the `.disabled(dashboardURL == nil)` modifier is unchanged.
    - `providerRowView_d11SubtitlePreservedForNonLocals` — Plan 03-07 STATE #91 preserved: source still contains `"Updated"` + `"usage temporarily unavailable"`.

    Use `@MainActor` annotation on suites that touch SwiftUI body construction; use `nonisolated` for source-walk helpers (Phase 3 STATE #91 lesson — Rule-1 fix during Plan 03-07).
  </action>
  <acceptance_criteria>
    - `xcodebuild test -only-testing:AgentsUsageBarTests/LocalRowSecondaryViewTests -only-testing:AgentsUsageBarTests/ProviderRowViewLocalRowTests` reports green.
    - Combined `@Test` case count ≥ 19.
    - **Strict LOCAL-06 enforcement test**: `noLocal06Leak_secondaryTextNeverIncludesUSD` passes — the view layer NEVER emits `tokens` / `USD` / `$` / `bal` for local rows.
    - **Source-walk invariant**: `providerRowView_local06AntiFeature_localsNeverReachTokenText` passes — locals are routed AWAY from `tokenText` / `usdText` / `balanceText` by the `if isLocal {` branch.
  </acceptance_criteria>
</task>

<task id="T-04-07-04">
  <title>Wire new files into `project.pbxproj` (UUID namespace `AA040700*`)</title>
  <type>execute</type>
  <read_first>
    - AgentsUsageBar.xcodeproj/project.pbxproj — Phase 3 Plan 03-07 UUID style at lines 156-164 (AA030700*)
  </read_first>
  <action>
    UUID namespace `AA040700xxxxxxxxxxxxxxxxxx`. Wire:

    Production:
    - `AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift` (UUID `AA040700000000000000001A` / `001B`)

    Tests:
    - `LocalRowSecondaryViewTests.swift` (UUID `AA040700000000000000010A` / `010B`)
    - `ProviderRowViewLocalRowTests.swift` (UUID `AA040700000000000000011A` / `011B`)

    Edits to `UI/ProviderRowView.swift` use the existing pbxproj reference — no new entry.
  </action>
  <acceptance_criteria>
    - `grep -c 'AA040700' AgentsUsageBar.xcodeproj/project.pbxproj` returns ≥ 6 (1 prod + 2 tests, doubled for ref + build entries).
    - `xcodebuild build -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -configuration Debug` exits 0.
  </acceptance_criteria>
</task>

## Verification

```
1. xcodebuild build -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -configuration Debug
2. xcodebuild test -only-testing:AgentsUsageBarTests/LocalRowSecondaryViewTests \
                   -only-testing:AgentsUsageBarTests/ProviderRowViewLocalRowTests
3. xcodebuild test  # full regression — Phase 1/2/3 ProviderRowView*Tests + new local row tests all green
4. grep -E 'tokens|USD|\$\b|bal\b|costToday' AgentsUsageBar/UI/Components/LocalRowSecondaryView.swift  # MUST return 0 (LOCAL-06 at UI)
5. grep 'degradedTag\|usage-temporarily-unavailable' AgentsUsageBar/Providers/Ollama AgentsUsageBar/Providers/LMStudio AgentsUsageBar/Providers/LlamaCpp  # MUST return 0 (locals don't stamp the D-11 marker)
6. grep 'arrow.up.right.square' AgentsUsageBar/UI/ProviderRowView.swift  # MUST return 1 (Plan 03-07 invariant preserved)
```

## must_haves

- For `isLocal=true` rows, ProviderRowView renders `LocalRowSecondaryView` in place of the tokens/USD/balance HStack — LOCAL-06 anti-feature enforced at the rendering layer (ROADMAP Phase 4 SC#5 explicit).
- All five D-03 row states (A/B/C/D/E) + state B' (degenerate) + the D-04 placeholderMessage path render the documented secondary-line strings — locked by 14+ `@Test` cases on the `secondaryText` accessor.
- StatusDot `.notRunning` → muted-gray (already landed in Plan 04-01); ProviderRowView passes lastStatus unchanged → status dot renders gray for not-running rows (ROADMAP Phase 4 SC#4 "muted Not running").
- Phase 3 invariants preserved: dashboard button visible-but-disabled for locals (D-13 + D-14 + STATE #89); D-11 degraded subtitle still fires for non-local rows.
- Local actors do NOT stamp `raw["note"] = "usage-temporarily-unavailable"` — verified by negative grep (degradedTag is Gemini-specific; locals have no quota so threshold notifications never fire anyway).

## Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| `ProviderID.localIDs.contains(state.id)` is O(N) on a Set — irrelevant at N=3 but adding more local IDs in v2 (EXP-01) could matter | Phase 4 ships with three local IDs; O(3) hash lookup is trivial. v2 expansion (Jan, vLLM) would still fit within Set tolerance. |
| ProviderRowView is `@MainActor`-bound; new `LocalRowSecondaryView` is `View` (implicitly MainActor) | Default `View` body is `@MainActor` per Swift 6 SwiftUI propagation. No concurrency hazard. |
| `secondaryText` accessor exposed as `internal` leaks implementation detail | Phase 1 STATE #30 precedent (`QuotaBar.color(forFraction:)` exposed `internal static` as test seam). Same pattern, same justification. The `internal` access stays within the module. |
| State E fires while another state should win (precedence ambiguity) | State E (`loadingModel == "true"`) is checked BEFORE State A (`.notRunning`) per the helper code — rationale: if `/health` returns 200 with "loading model" AND `/v1/models` succeeds, the row is clearly "Running — loading" not "Not running". Locked by `stateE_takesPrecedenceOverModelCount`. |
| The view shows the empty fallback `"—"` for a misconfigured local provider | Fallback rendered only when count > 0 but `modelName == nil` (impossible — if count > 0, the actor always stamps modelName). Defensive but unreachable in practice; locked by `unknownState_fallbackToDashEm`. |
| Plan 03-07's dashboard button visibility breaks for locals | Phase 3 STATE #89 explicit: `ProviderDashboardURL.lookup(.ollama)` returns nil → button is `.disabled` but VISIBLE. Plan 04-07 doesn't touch the button rendering; verified by source-grep `arrow.up.right.square` returns 1. |
| placeholderMessage stamped on a non-local provider (Gemini, Codex) accidentally renders via LocalRowSecondaryView | The `isLocal` gate is the outermost branch; placeholderMessage on non-local rows continues to render via the Phase 1 default behavior (currently no UI surfaces placeholderMessage for non-local rows — Plan 04-07 doesn't change that). Plan 04-08 will only set placeholderMessage on the llama.cpp placeholder seed. |
