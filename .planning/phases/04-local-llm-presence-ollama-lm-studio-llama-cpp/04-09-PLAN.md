---
plan_id: 04-09
phase: 04
title: Phase 4 UAT — 5 manual + 5 unit-test attestation walkthrough mirroring Phase 2 / Phase 3 reviewer-signal protocol
wave: 4
depends_on: [04-07, 04-08]
files_modified:
  - .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md
requirements: []
autonomous: false
estimated_duration_min: 45
---

## Objective

Author the Phase 4 User Acceptance Test script — `.planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md` — a reviewer-runnable Markdown document mirroring the Phase 2 `02-UAT.md` + Phase 3 `03-UAT.md` shape. Tests 1-5 are **manual** verification of the five ROADMAP Phase 4 success criteria; Tests 6-10 are **unit-test attestation** citing the named Wave 1-3 suites that already passed during Plan 04-04 / 04-05 / 04-06 / 04-07 / 04-08 execution.

The Phase 2 pattern (Tests 1-2 manual + Tests 3-10 attested → reviewer signed `approved` after ~30min) and Phase 3 pattern (5 manual + 5 attested, 4 hotfixes landed mid-UAT) both proved the format. Phase 4 follows verbatim.

Plan 04-09 is the **final phase exit gate** — code-complete after Plans 04-01..08; reviewer signal in this UAT determines whether Phase 4 transitions to "approved" or to gap closure via `/gsd-plan-phase 04 --gaps`.

## Context

Phase 2 STATE #117 + Phase 3 03-09 set the protocol:
- 10-row outcomes table with `# / Test / Result / Notes` columns; `<pending>` placeholders the reviewer fills in.
- Manual rows 1-5; attestation rows 6-10.
- Pre-conditions checklist adapted to phase-specific setup (Phase 4: install Ollama; install LM Studio; install llama.cpp / llamafile).
- Reviewer-signal protocol: `approved` / `failed: <test#>: <description>` / `deferred: <test#>: <reason>`.
- Hotfixes table stub (Phase 3 G-01..G-04 precedent — landed 4 in-session fixes).

ROADMAP Phase 4 success criteria (`.planning/ROADMAP.md` lines 99-104):
1. Ollama row populates from `/api/ps` + `/api/tags` with 1–2s connect timeout.
2. LM Studio row populates with port override via `[lmstudio].port` in config.toml.
3. llama.cpp row activates only with explicit `[llamacpp].port`; probes `/health`, `/slots`, `/v1/models`.
4. Connection-refused → muted "Not running"; never red; never blocking other providers.
5. Local rows NEVER display cumulative tokens (LOCAL-06).

Manual Tests 1-5 map 1:1 to these criteria. Tests 6-10 attest the named Plan 04-04 / 04-05 / 04-06 / 04-07 / 04-08 test suites.

## Tasks

<task id="T-04-09-01">
  <title>Author `04-UAT.md` — 10-test walkthrough script mirroring 02/03 UAT shape</title>
  <type>execute</type>
  <read_first>
    - .planning/phases/02-claude-provider-threshold-rollover-jsonl-streaming/02-UAT.md (Phase 2 — the authoritative shape; 02-UAT.md table format + pre-conditions + per-test PASS criteria + Hotfixes table + Reviewer signal block)
    - .planning/phases/03-remote-api-providers-codex-gemini/03-UAT.md (Phase 3 — 4-gap-closure precedent; 10-row table; manual + attestation split)
    - .planning/ROADMAP.md §"Phase 4" — the five success criteria verbatim
    - .planning/REQUIREMENTS.md — LOCAL-01..06 verbatim text
    - 04-CONTEXT.md D-01..D-04
    - 04-RESEARCH.md §1 (Requirement Anchoring) + §8 (Test Strategy)
    - 04-01-PLAN.md..04-08-PLAN.md — names of the test suites attested by Tests 6-10
  </read_first>
  <action>
    Write `.planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md`. Match `03-UAT.md` structure byte-for-byte where possible (only test contents change).

    ### Header
    ```
    # Phase 4 — User Acceptance Test

    **Reviewer:** Siriwat Uamngamsup
    **Date:** <fill-in at run time>
    **Build:** <latest git SHA at UAT start>
    ```

    ### Test outcomes table
    Ten-row table with columns `# | Test | Result | Notes`. Pre-populate Result column with `<pending>`. Header text mirrors `03-UAT.md` — `PASS / DEFER / FAIL / PARTIAL` cell values.

    Rows 1-5 (manual):
    1. Ollama row populates from /api/ps + /api/tags with 1–2s timeout (SC #1 — LOCAL-01 + LOCAL-04)
    2. LM Studio row populates with port override (SC #2 — LOCAL-02 + LOCAL-04)
    3. llama.cpp row activates only when port configured; D-04 placeholder when unconfigured (SC #3 — LOCAL-03 + LOCAL-04 + D-04)
    4. Connection-refused renders muted "Not running" — never red — does not block other providers (SC #4 — LOCAL-04 + LOCAL-05)
    5. No cumulative-token count ever displayed on any local row (SC #5 — LOCAL-06 anti-feature enforcement)

    Rows 6-10 (unit-test attestation):
    6. ProviderStatus.notRunning + classifyLocalhost classifier + StatusDot.gray (attested by ProviderStatusNotRunningTests + ProviderErrorLocalhostClassifierTests + StatusDotNotRunningTests + ProviderIDLocalConstantsTests — Plan 04-01)
    7. Config layer — AppConfig + ConfigStore [ollama] / [lmstudio] / [llamacpp] parsing (attested by AppConfigLocalProvidersTests + ConfigStoreLocalSectionsTests — Plan 04-02)
    8. Per-runtime probe actors — OllamaProvider + LMStudioProvider + LlamaCppProvider with lenient Codable + never-throws-transient (attested by OllamaProviderTests + LMStudioProviderTests + LlamaCppProviderTests + their Codable suites — Plans 04-04, 04-05, 04-06)
    9. UI row-state rendering — LocalRowSecondaryView five states + ProviderRowView branching (attested by LocalRowSecondaryViewTests + ProviderRowViewLocalRowTests — Plan 04-07)
    10. Composition + LOCAL-06 enforcement + ThresholdEngine skip — second URLSession tier + three registrations + D-04 placeholder + nil-quota guard (attested by AppDependenciesLocalRegistrationTests + ThresholdEngineLocalNilQuotaTests + AggregateStoreLocalRollupTests — Plan 04-08)

    Below the table:
    `**Overall outcome:** <APPROVED / FAILED / DEFERRED — fill in after Tests 1-5>.`

    ### Pre-conditions section
    Reproduce the 02-UAT + 03-UAT structure, adapted for Phase 4:
    - `xcodebuild build -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -configuration Debug` exits 0.
    - Launch the app from `~/Library/Developer/Xcode/DerivedData/AgentsUsageBar-*/Build/Products/Debug/AgentsUsageBar.app`.
    - **For Test 1 (Ollama):** Install Ollama from https://ollama.com; run `ollama serve` in a terminal; pre-pull at least one model via `ollama pull llama3:8b`; verify `curl http://localhost:11434/api/ps` returns JSON.
    - **For Test 2 (LM Studio):** Install LM Studio from https://lmstudio.ai; load at least one model in the LM Studio UI; verify the local server is enabled at port 1234 (default) OR set a custom port in LM Studio AND add `[lmstudio]\nport = <custom>` to `~/.config/agents-usage-bar/config.toml`.
    - **For Test 3 (llama.cpp):** Either install via `brew install llama.cpp` or download `llamafile` from https://github.com/Mozilla-Ocho/llamafile; start a server with `./llama-server -m <model.gguf> --port 8080`; add `[llamacpp]\nport = 8080` to config.toml.
    - **For Test 4 (connection-refused):** ensure each runtime can be stopped (e.g. `pkill -f ollama serve`).
    - Keep Phase 1/2/3 pre-conditions satisfied (OPENROUTER_API_KEY env var; ~/.claude transcripts; ~/.codex/sessions; ~/.gemini/oauth_creds.json) so the full popover regression remains observable.
    - Recommended: open a second Terminal window with `log stream --predicate 'subsystem == "app.agents-usage-bar" AND (category == "ollama" OR category == "lmstudio" OR category == "llamacpp")' --info` running.

    ### Per-test sections

    For each manual test, write a numbered step list with explicit PASS criteria. Match Phase 2/3 specificity. Concretely:

    **Test 1 — Ollama row populates (Phase 4 SC #1 / LOCAL-01)**
    1. Open the popover. Verify an "Ollama" row exists.
    2. PASS if Ollama row shows the model name within 5s (e.g. `llama3:8b`).
    3. PASS if VRAM suffix renders when `/api/ps` returns non-zero `size_vram` (e.g. `llama3:8b · 4.8 GB VRAM`).
    4. PASS if rolling `ollama run llama3:8b "test"` in a second terminal loads the model and the row updates within ≤ 5 minutes (or sooner via popover-open refresh per POLL-03).
    5. PASS if `log stream --predicate 'subsystem == "app.agents-usage-bar" AND category == "ollama"'` shows a `GET /api/ps → 200` line every poll interval (default 5 min).
    6. PASS if connection-refused (after `pkill -f "ollama serve"`) flips the row to "Not running" within 1 poll, with no error styling. (Sub-step also covered by Test 4.)

    **Test 2 — LM Studio row + port override (Phase 4 SC #2 / LOCAL-02)**
    1. Open the popover. Verify an "LM Studio" row exists.
    2. PASS if LM Studio row shows the loaded model id (e.g. `meta-llama-3.1-8b-instruct`).
    3. PASS if `log stream … category == "lmstudio"` shows `GET /api/v0/models → 200` (Plan 04-05 extended endpoint).
    4. PASS if loading additional models in LM Studio's UI updates the secondary line within one poll cycle.
    5. **Port override sub-test:** quit the app; add `[lmstudio]\nport = 4321` to `~/.config/agents-usage-bar/config.toml`; restart LM Studio on port 4321; relaunch the app. PASS if the row populates from port 4321 (verify via log stream that `localhost:4321` URLs are emitted, not `localhost:1234`).
    6. **Fallback sub-test (optional):** if using older LM Studio (< 0.3.5) without `/api/v0/models`, PASS if the row STILL populates via the `/v1/models` fallback (Plan 04-05 OQ-2 path). Mark DEFERRED if no older build available.

    **Test 3 — llama.cpp row activates only with port + D-04 placeholder (Phase 4 SC #3 / LOCAL-03 + D-04)**
    1. **Unconfigured baseline:** Without `[llamacpp]` section in config.toml, open popover. PASS if "llama.cpp" row appears with the **exact** subtitle `"Set [llamacpp] port in config.toml to enable"` (D-04 verbatim) and a muted gray status dot (.notRunning).
    2. PASS if NO probe to any localhost port appears in `log stream` for category "llamacpp" — LOCAL-03 no-scanning invariant.
    3. **Configured probe path:** Add `[llamacpp]\nport = 8080` to config.toml; start `./llama-server -m <model.gguf> --port 8080`; quit the app; relaunch. PASS if the llama.cpp row populates with the model basename (e.g. `llama-3-8b-instruct-Q4_K_M.gguf`).
    4. PASS if `log stream … category == "llamacpp"` shows requests to `/health`, `/v1/models`, and `/slots` per poll cycle.
    5. **State E (loading model) sub-test:** restart llama-server with a large model (e.g. 70B); during the cold load window (10-60s), open popover. PASS if the row reads `"Running — loading model…"` (not "Not running", not red). After model finishes loading, row should transition to `"<model-basename>"` within one poll.
    6. **Port absent → placeholder:** remove `[llamacpp].port` from config.toml; restart the app. PASS if the row reverts to the D-04 unconfigured placeholder (subtitle + muted dot).

    **Test 4 — Connection-refused → muted "Not running"; never red; never blocking (Phase 4 SC #4 / LOCAL-04 + LOCAL-05)**
    1. With all three local runtimes RUNNING (rows populated), confirm each shows model name + green-or-neutral status dot.
    2. **Stop Ollama:** `pkill -f "ollama serve"`. Click Refresh in the popover footer (or wait 1 poll). PASS if the Ollama row transitions to "Not running" with a **gray** status dot — NEVER **red**, NEVER an error subtitle.
    3. PASS if OTHER provider rows (OpenRouter, Claude, Codex, Gemini, LM Studio, llama.cpp) continue refreshing normally — their "Updated Xs ago" labels tick forward; their values change as the underlying sources do. (Cross-provider isolation per GEMINI-04 generalised — Phase 3 STATE #82 + Phase 4 actors NEVER throw for transient flakiness.)
    4. **Stop LM Studio:** quit LM Studio app. PASS if its row also transitions to "Not running" gray within one poll, other rows unaffected.
    5. **Stop llama.cpp:** `pkill -f "llama-server"`. PASS if its row goes to "Not running" gray, other rows unaffected.
    6. PASS if `log stream` shows NO `.error` log-level lines for the stopped runtimes — only `.notice` at most.
    7. PASS if forcing a quota fraction crossing on a different provider (e.g. OpenRouter at 85%) still fires a notification for THAT provider — the locals' "Not running" state never blocks unrelated threshold notifications. (If reviewer cannot force this manually, mark DEFERRED with attestation of `ThresholdEngineLocalNilQuotaTests`.)
    8. **Restart and recover:** `ollama serve &` in background; click Refresh. PASS if the Ollama row returns to its normal model-name state within one poll cycle.

    **Test 5 — No cumulative-token count ever displayed on local rows (Phase 4 SC #5 / LOCAL-06 anti-feature)**
    1. With all three local runtimes running and rendering model names, scrutinise every local row carefully.
    2. PASS if NO local row shows ANY of the following substrings: `"tokens"`, `"USD"`, `"$"`, `"bal"`, `"K tokens"`, `"M tokens"`. The ONLY secondary-line substring acceptable on a local row is one of:
       - `"Not running"`
       - `"Idle — 0 models loaded"`
       - `"Idle — no models installed"`
       - `"<modelName>"` optionally with `· X.X GB VRAM`
       - `"<modelName> · +N more"`
       - `"Running — loading model…"`
       - `"Set [llamacpp] port in config.toml to enable"` (D-04 placeholder)
    3. PASS if running `ollama run llama3:8b "Tell me a joke. Use lots of tokens."` for ≥30 seconds does NOT cause the row to display any token figure.
    4. PASS if the "Today total" header at the popover top continues to reflect remote-only totals (Phase 3 D-07 — local providers excluded from rollup).
    5. PASS if the "Total excludes quota-only providers" footnote under the totals row continues firing (Phase 3 STATE #92 + Plan 04-08 — `hasAnyQuotaOnlyProvider` returns true when any local is registered).
    6. **Source audit (optional but recommended):** the reviewer can run `grep -rE 'tokensToday|tokenCount|costTodayUSD' AgentsUsageBar/Providers/{Ollama,LMStudio,LlamaCpp}/` and assert 0 matches. The plan's automated invariants already enforce this; this manual step is an extra layer of confidence.

    ### Tests 6-10 — Unit-test attestation table
    Attestation paragraphs mirroring 02-UAT / 03-UAT shape. Each row cites named test suites + the SUMMARY file documenting them:

    > **Test 6 — `ProviderStatus.notRunning` foundation + URLError classifier + StatusDot.gray:** Attested by `ProviderStatusNotRunningTests` (3+ cases), `ProviderIDLocalConstantsTests` (6+ cases incl. `localIDs` Set carrier), `ProviderErrorLocalhostClassifierTests` (8+ parameterised cases: `.cannotConnectToHost` / `.cannotFindHost` / `.networkConnectionLost` / `.timedOut` → `.notRunning`; `.notConnectedToInternet` / HTTP 500 / DecodingError → `.error`), `StatusDotNotRunningTests` (4+ cases). RESEARCH §3.1 mapping locked at compile time. **POLL-06 non-terminal invariant**: `grep -E 'case \.notRunning' AgentsUsageBar/Aggregation/AggregateStore.swift` returns 0 (the case is deliberately ABSENT from the terminal-skip block). See `04-01-SUMMARY.md`.

    > **Test 7 — Config layer for the three local sections:** Attested by `AppConfigLocalProvidersTests` (7+ cases verifying defaults `OllamaConfig(enabled: true)`, `LMStudioConfig(enabled: true, port: 1234)`, `LlamaCppConfig(enabled: true, port: nil)`) + `ConfigStoreLocalSectionsTests` (8+ cases incl. TOML disables, port overrides, garbage-fail-soft, regression against Phase 1+3 surface). Phase 1 D-17 env > toml > defaults precedence preserved; no env override for locals per CONTEXT Discretion (no secrets). See `04-02-SUMMARY.md`.

    > **Test 8 — Per-runtime probe actors:** Attested by:
    > - `OllamaProviderTests` (18+ cases — happy path single-model + multi-model D-03 +N-more + Idle B/B' + connection-refused → notRunning + tags-failure-doesn't-abort + LOCAL-06 source-grep + D-12 no-CircuitBreaker + bearer-always-nil). See `04-04-SUMMARY.md`.
    > - `LMStudioProviderTests` (20+ cases — extended endpoint + /v1/models fallback on 404 AND 500 + state-aware filtering on `state == "loaded"` + port override propagation). See `04-05-SUMMARY.md`.
    > - `LlamaCppProviderTests` (24+ cases — `/health` "ok"/"loading model"/"error"/"no slot available" + basename trimming + opportunistic `/slots` ignored on failure + lenient unknown-status → .ok per OQ-3). See `04-06-SUMMARY.md`.
    > - Plus the three Codable suites (`OllamaResponsesCodableTests`, `LMStudioResponsesCodableTests`, `LlamaCppResponsesCodableTests` — each 7-11 cases) confirming lenient decoders tolerate unknown future fields per Phase 2 STATE #37 / Pitfall 7.
    > Combined LOCAL-06 enforcement: every actor's source contains 0 token / cost references — negative-grep gate is part of each plan's acceptance criteria.

    > **Test 9 — UI row-state rendering:** Attested by `LocalRowSecondaryViewTests` (14+ cases — one per row state A/B/B'/C/D/E + placeholderMessage precedence + VRAM suffix formatting + LOCAL-06 invariant `noLocal06Leak_secondaryTextNeverIncludesUSD`) + `ProviderRowViewLocalRowTests` (5+ source-walk cases — `if isLocal { LocalRowSecondaryView } else { … tokenText … }` branching pattern locked). StatusDot.gray for `.notRunning` already locked by Test 6. See `04-07-SUMMARY.md`.

    > **Test 10 — Composition + cross-cutting policy:** Attested by `AppDependenciesLocalRegistrationTests` (7+ cases incl. `appDependenciesMakeProduction_smokeTest` cross-environment composition-root check + `seedPlaceholder_llamacppWithSubtitle` D-04 verbatim) + `ThresholdEngineLocalNilQuotaTests` (5+ cases — locals with `quota: nil` produce 0 NotificationDecisions; mixed local + remote with .85 fraction emits exactly 1 decision for the remote; Pitfall 6 lock) + `AggregateStoreLocalRollupTests` (3+ cases — D-07 rollupTotals exclusion regression; `hasAnyQuotaOnlyProvider` returns true when any local is registered). See `04-08-SUMMARY.md`.

    ### Final section — Reviewer signal
    ```
    ## Reviewer signal

    Reply with one of:

    - `approved` — Phase 4 complete; the executor will commit the marked-up 04-UAT.md, update STATE.md + ROADMAP.md, and the orchestrator can run `/gsd-transition` to advance to Phase 5 (First-Run UX + Settings Polish).
    - `failed: <test#>: <description>` — File one or more gap entries. The executor records the failed steps in this 04-UAT.md, leaves Phase 4 in BLOCKED state, and the next action is `/gsd-plan-phase 04 --gaps` to plan closure.
    - `deferred: <test#>: <reason>` — Accept the test as unit-test-attested only (mirrors Phase 2/3 reviewer accepting attestation rows). Phase 4 still considered complete if Tests 1-5 manual or 6-10 attested.
    ```

    Add a "Hotfixes landed during the UAT session" subsection — empty table with the columns `Commit | Subject` (mirrors Phase 2's `378c531 / c84c452 / 4a5ebee` and Phase 3 G-01..G-04 patterns). Reviewer fills in if regressions surface during manual walkthrough.

    Add a final "Phase 4 Success Criteria Mapping" table mirroring 03-UAT.md final section:
    ```
    | Success Criterion                                                                                            | UAT Tests       |
    |--------------------------------------------------------------------------------------------------------------|-----------------|
    | #1 — Ollama row from /api/ps + /api/tags with 1–2s timeout (LOCAL-01 + LOCAL-04)                              | Test 1, 6, 8    |
    | #2 — LM Studio row + port override (LOCAL-02 + LOCAL-04)                                                       | Test 2, 7, 8    |
    | #3 — llama.cpp only with port + D-04 placeholder (LOCAL-03 + LOCAL-04 + D-04)                                  | Test 3, 7, 8, 10 |
    | #4 — Connection-refused muted, never blocking (LOCAL-04 + LOCAL-05)                                            | Test 4, 6, 8    |
    | #5 — LOCAL-06 anti-feature — no token count ever (LOCAL-06)                                                    | Test 5, 8, 9, 10 |
    ```
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md` exists and is non-empty.
    - File contains the literal heading "# Phase 4 — User Acceptance Test".
    - File contains a 10-row test outcomes table (`grep -c '^| [0-9]' 04-UAT.md` returns ≥ 10).
    - File contains per-test sections for Tests 1-5 (each starting with a level-3 or bold heading mentioning the test name + Phase 4 SC reference).
    - File references the D-04 verbatim subtitle string `"Set [llamacpp] port in config.toml to enable"` at least once.
    - File references all six LOCAL requirement IDs (LOCAL-01..06).
    - File references at least 12 named unit-test suites for Tests 6-10 attestation (every Plan 04-01..08 named suite from the plans).
    - File contains a "Reviewer signal" section with the `approved` / `failed` / `deferred` reply protocol.
    - File contains a "Hotfixes landed during the UAT session" subsection.
    - File contains a Phase 4 Success Criteria Mapping table.
  </acceptance_criteria>
</task>

<task id="T-04-09-02">
  <title>CHECKPOINT — Reviewer runs 04-UAT.md and replies with signal</title>
  <type>checkpoint:human-verify</type>
  <read_first>
    - 04-UAT.md (authored in T-04-09-01)
  </read_first>
  <action>
    The executor has produced `.planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md` — a 10-test walkthrough script. Tests 1-5 are manual (~30-45 min total assuming Ollama + LM Studio + llama.cpp pre-conditions are already satisfied per the script's Pre-conditions section). Tests 6-10 are unit-test attestation entries citing the named suites that passed during Plans 04-01..08.

    The full xcodebuild test suite (Plans 04-01..08 + Phase 1/2/3 regression) is green at this point — Plan 04-08's Task 4 acceptance criterion required it.

    **How to verify:**
    1. Open `.planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md`.
    2. Verify pre-conditions are satisfied (especially: Ollama running with a pulled model; LM Studio running with a loaded model; llama.cpp server running on a configured port; OPENROUTER_API_KEY env set for cross-provider total visibility).
    3. Run Tests 1-5 in order. Mark each row's Result column as PASS / DEFER / FAIL / PARTIAL with a brief Note (e.g. "PASS — Ollama showed `llama3:8b · 4.8 GB VRAM` within 3s; pkill of ollama serve produced gray Not running within 5min poll, OpenRouter row continued refreshing normally").
    4. For Tests 6-10, scan the attestation table and verify each named unit-test suite is referenced in the corresponding SUMMARY file. Mark as DEFER with the same attestation language Phase 2/3 used.
    5. Reply with one of:
       - `approved` — Phase 4 complete. Executor commits the filled-in 04-UAT.md, updates STATE.md + ROADMAP.md to mark Phase 4 complete, and the orchestrator can run `/gsd-transition` to advance to Phase 5 (First-Run UX + Settings Polish).
       - `failed: <test#>: <description>` — File one or more gap entries. Executor records the failed steps in 04-UAT.md, leaves Phase 4 in BLOCKED state, and the next action is `/gsd-plan-phase 04 --gaps`.
       - `deferred: <test#>: <reason>` — Accept the test as unit-test-attested only (mirrors Phase 2/3 reviewer accepting Tests 3-10 as attested).
  </action>
  <acceptance_criteria>
    - Reviewer's reply signal is captured in `04-09-SUMMARY.md` verbatim (mirrors Phase 3 03-09-SUMMARY.md pattern).
    - STATE.md and ROADMAP.md updated to reflect Phase 4 completion (or BLOCKED for gap closure if `failed`).
    - Any hotfix commits landed during the UAT session are recorded in the "Hotfixes landed during the UAT session" subsection of 04-UAT.md.
  </acceptance_criteria>
</task>

## Verification

```
1. test -f .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md  # file exists
2. grep -c '^| [0-9]' .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md  # ≥ 10 (10-row table)
3. grep -c '^### Test\|^\*\*Test' .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md  # ≥ 5 per-test sections
4. grep 'Set \[llamacpp\] port in config.toml to enable' .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md  # ≥ 1 (D-04 verbatim)
5. grep -E 'LOCAL-0[1-6]' .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md  # ≥ 6 (one per requirement ID)
6. grep '## Reviewer signal' .planning/phases/04-local-llm-presence-ollama-lm-studio-llama-cpp/04-UAT.md  # ≥ 1
```

## must_haves

- 04-UAT.md authored matching the Phase 2/3 02-UAT/03-UAT structure (10-row table, 5 manual + 5 attestation, pre-conditions checklist, reviewer-signal protocol, Hotfixes stub, Phase 4 Success Criteria Mapping).
- Tests 1-5 runnable in ~30-45 minutes by a reviewer with Ollama + LM Studio + llama.cpp pre-installed and pre-conditions satisfied.
- Tests 6-10 attestation table cites every named test suite from Plans 04-01..08.
- D-04 verbatim subtitle string `"Set [llamacpp] port in config.toml to enable"` appears in Test 3 PASS criteria.
- LOCAL-06 anti-feature verification in Test 5 lists EVERY acceptable secondary-line string for local rows — the reviewer can confirm by visual scan.
- Reviewer signal captured + Phase 4 either approved or routed to gap closure.

## Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Reviewer doesn't have all three local runtimes installed | Phase 2/3 precedent: any test can be marked DEFERRED with the appropriate unit-test-attestation cite. The plan ensures attestation coverage for every manual test. |
| llama.cpp UAT is hardest to set up (manual model download + manual server start) | Test 3 is independent of Tests 1-2; reviewer can DEFER Test 3 manually if llama.cpp setup is too time-consuming. The plan explicitly says "Mark as DEFERRED with `LlamaCppProviderTests` attestation if reviewer can't allocate setup time." |
| Discovery of a regression during manual walkthrough (Phase 3 G-01..G-04 precedent: 4 gaps surfaced) | Plan 04-09 mirrors the 03-UAT.md "Gaps" filing pattern. Hotfix table is empty by default; reviewer fills in any in-session fixes. The plan does NOT pre-suppose the manual walkthrough will pass — it explicitly accommodates `failed: <test#>: <description>` replies. |
| Phase 4 ROADMAP success criteria misalignment with the UAT tests | RESEARCH §1 anchored each LOCAL-01..06 to a satisfying plan; UAT tests follow the same anchoring. Test 5 specifically verifies LOCAL-06 by enumerating acceptable secondary-line strings (negative space). |
| Reviewer can't visually distinguish state E "Running — loading model…" from state B "Idle — 0 models loaded" — the model load is fast | Test 3 step 5 explicitly says "restart llama-server with a large model (e.g. 70B)" to ensure the load window is observable. Smaller models load too fast — reviewer should pick a 30+ GB model. Alternative: mark DEFERRED with `LlamaCppProviderTests.loadingModelHealth_yieldsStateE` attestation. |
| Test 4 cross-provider isolation sub-step 7 (force OpenRouter at 85% to fire a notification while a local is "Not running") is hard to set up | DEFERRED-acceptable per the plan; `ThresholdEngineLocalNilQuotaTests` attestation covers the same invariant via stub snapshots. |
| Plan 04-09 is `autonomous: false` per frontmatter — executor cannot self-approve | Mirrors Phase 3 03-09: the executor authors the UAT script and stops; the reviewer's signal is the gate. STATE/ROADMAP do NOT mark Phase 4 complete until the reviewer signals `approved` (Phase 2 precedent: 4895ae8 shipped 02-UAT.md, a442a0a four days later carried the approval close-out). |
