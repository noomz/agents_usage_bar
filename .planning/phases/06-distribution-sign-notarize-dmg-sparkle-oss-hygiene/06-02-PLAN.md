---
phase: 06-distribution-sign-notarize-dmg-sparkle-oss-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AgentsUsageBar.xcodeproj/project.pbxproj
  - AgentsUsageBar.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - AgentsUsageBar/Resources/Info.plist
  - AgentsUsageBar/App/AgentsUsageBarApp.swift
  - ExportOptions.plist
autonomous: true
requirements: [REL-06]

must_haves:
  truths:
    - "The app builds with Sparkle 2.9.2 as an SPM dependency embedded & signed in the app target."
    - "Info.plist declares SUFeedURL (GitHub Pages appcast URL) and SUPublicEDKey (placeholder until generate_keys is run)."
    - "The pbxproj exposes CURRENT_PROJECT_VERSION so CI agvtool can inject the integer build number Sparkle compares against (CFBundleVersion)."
    - "ExportOptions.plist exists at repo root with method=developer-id + hardenedRuntime so the CI export re-signs Sparkle helpers correctly."
  artifacts:
    - path: "AgentsUsageBar/Resources/Info.plist"
      provides: "SUFeedURL + SUPublicEDKey Sparkle update keys"
      contains: "SUFeedURL"
    - path: "AgentsUsageBar/App/AgentsUsageBarApp.swift"
      provides: "SPUStandardUpdaterController wired at app init"
      contains: "SPUStandardUpdaterController"
    - path: "ExportOptions.plist"
      provides: "Developer ID export options for CI -exportArchive"
      contains: "developer-id"
  key_links:
    - from: "AgentsUsageBarApp"
      to: "Sparkle"
      via: "import Sparkle + SPUStandardUpdaterController(startingUpdater: true, ...)"
      pattern: "import Sparkle"
    - from: "Info.plist SUFeedURL"
      to: "GitHub Pages appcast.xml"
      via: "https URL Sparkle polls on launch"
      pattern: "SUFeedURL"
---

<objective>
Wire Sparkle 2.9.2 into the app: add it as an exact-version SPM dependency embedded-and-signed in the app target, instantiate `SPUStandardUpdaterController` at app init, add `SUFeedURL` + `SUPublicEDKey` to Info.plist, expose `CURRENT_PROJECT_VERSION` in pbxproj for CI build-number injection, and commit a Developer-ID `ExportOptions.plist`.

Purpose: This is the app-side half of the auto-update feature (REL-06). The CI side (appcast signing/publishing) is plan 06-04; this plan makes the app capable of checking and applying updates against the appcast.
Output: A buildable app embedding Sparkle, Info.plist update keys, version build settings ready for `agvtool`, and the export options the release workflow consumes. App Sandbox stays OFF, Hardened Runtime stays ON, entitlements UNCHANGED (D-08).
</objective>

<execution_context>
@$HOME/.ccs/instances/personal/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-CONTEXT.md
@.planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-RESEARCH.md

<interfaces>
<!-- Existing app entry + current pbxproj/Info.plist state. -->

From AgentsUsageBar/App/AgentsUsageBarApp.swift:
- `@main struct AgentsUsageBarApp: App` with an `init()` (already calls `setActivationPolicy(.accessory)` and `UNNotificationManager.registerCategories`).
- `@State private var dependencies: Dependencies = AppDependencies.makeProduction()`.
- Body has `MenuBarExtra { ... }` and a `Settings { ... }` scene with a `.commands { CommandGroup(replacing: .appSettings) ... }`.

From AgentsUsageBar.xcodeproj/project.pbxproj (verified):
- `objectVersion = 77` (D-13; Sparkle SPM is compatible).
- `MARKETING_VERSION = 0.1.0` present on all configs; `PRODUCT_BUNDLE_IDENTIFIER = app.agents-usage-bar`.
- `ENABLE_HARDENED_RUNTIME = YES`, `ENABLE_APP_SANDBOX = NO`, `CODE_SIGN_ENTITLEMENTS = AgentsUsageBar/Resources/Entitlements.plist`.
- `CURRENT_PROJECT_VERSION` is ABSENT (0 matches) and `VERSIONING_SYSTEM` is ABSENT — agvtool `new-version -all` needs `CURRENT_PROJECT_VERSION` (and `VERSIONING_SYSTEM = apple-generic`) present; this plan must add them (RESEARCH Pitfall 1 / Pitfall 8 + Assumption A2).

From AgentsUsageBar/Resources/Info.plist (verified):
- `CFBundleVersion = 1` (literal string, not a `$(CURRENT_PROJECT_VERSION)` variable) and `CFBundleShortVersionString = 0.1.0`. To let `agvtool` drive Sparkle's compared build number, Info.plist should reference `$(CURRENT_PROJECT_VERSION)` for `CFBundleVersion` and `$(MARKETING_VERSION)` for `CFBundleShortVersionString` (Pitfall 8).
- Already contains `NSAppTransportSecurity → NSAllowsLocalNetworking = true` (REL-08 — do NOT touch).

RESEARCH.md Pattern 1 (Sparkle SPM integration) + Pattern 3 (ExportOptions.plist) give the exact code/plist shapes — follow them. Sparkle pin: 2.9.2 (exact). SPM checksum verified in RESEARCH Sources: `b83e37436774556ed055e0244b297ef2c790e0737393bf65bf495fcbba6eed65`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sparkle 2.9.2 SPM dependency embedded & signed in the app target</name>
  <files>AgentsUsageBar.xcodeproj/project.pbxproj, AgentsUsageBar.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved</files>
  <read_first>
    - AgentsUsageBar.xcodeproj/project.pbxproj (locate: the `objectVersion=77` header; the app target's PBXNativeTarget; PBXFrameworksBuildPhase; the absence of any XCRemoteSwiftPackageReference / XCSwiftPackageProductDependency section — you are adding the first SPM dep)
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-RESEARCH.md (Pattern 1 — SPM steps + verified checksum + version pin)
  </read_first>
  <action>
    Add Sparkle as the project's first Swift Package dependency, pinned to EXACT version `2.9.2` from `https://github.com/sparkle-project/Sparkle`. Edit `project.pbxproj` to add: (1) an `XCRemoteSwiftPackageReference` (repositoryURL = the Sparkle GitHub URL; requirement kind = `exactVersion`, version = `2.9.2`); (2) an `XCSwiftPackageProductDependency` for product `Sparkle`; (3) a `packageReferences` array on the PBXProject object referencing the remote package; (4) a `packageProductDependencies` entry on the app's PBXNativeTarget; (5) a PBXBuildFile + PBXFrameworksBuildPhase entry so `Sparkle` is linked AND set to "Embed & Sign" (Sparkle is a framework with XPC helpers — it must be embedded, code-sign-on-copy ON). Use a fresh UUID namespace consistent with the project's existing convention. Create/update `project.xcworkspace/xcshareddata/swiftpm/Package.resolved` pinning Sparkle to 2.9.2 with the verified checksum so CI resolves deterministically. Do NOT add any other SPM packages. Do NOT modify Entitlements.plist (D-08 — Sparkle 2.x on non-sandboxed Hardened Runtime needs NO disable-library-validation; RESEARCH Anti-Patterns + sandboxing doc).
  </action>
  <verify>
    <automated>grep -n "Sparkle" AgentsUsageBar.xcodeproj/project.pbxproj | head && grep -n "2.9.2" AgentsUsageBar.xcodeproj/project.pbxproj AgentsUsageBar.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `project.pbxproj` contains an `XCRemoteSwiftPackageReference` for `sparkle-project/Sparkle` with `exactVersion` `2.9.2`.
    - `Sparkle` appears as an `XCSwiftPackageProductDependency` linked to the app target AND embedded (Embed & Sign / code-sign-on-copy).
    - `Package.resolved` pins Sparkle to `2.9.2`.
    - `Entitlements.plist` is byte-unchanged (no new entitlements): `git diff --stat AgentsUsageBar/Resources/Entitlements.plist` shows no change.
  </acceptance_criteria>
  <done>Sparkle 2.9.2 is an exact-pinned, embedded-and-signed SPM dependency of the app target; entitlements untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SPUStandardUpdaterController + Info.plist Sparkle keys + version build settings</name>
  <files>AgentsUsageBar/App/AgentsUsageBarApp.swift, AgentsUsageBar/Resources/Info.plist, AgentsUsageBar.xcodeproj/project.pbxproj</files>
  <read_first>
    - AgentsUsageBar/App/AgentsUsageBarApp.swift (the `@main struct ... init()` to extend; do not disturb the existing MenuBarExtra/Settings scenes)
    - AgentsUsageBar/Resources/Info.plist (CFBundleVersion / CFBundleShortVersionString lines + the existing NSAppTransportSecurity block to preserve)
    - AgentsUsageBar.xcodeproj/project.pbxproj (the XCBuildConfiguration dicts where MARKETING_VERSION lives — both Debug and Release for the app target — to add CURRENT_PROJECT_VERSION + VERSIONING_SYSTEM)
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-RESEARCH.md (Pattern 1 code snippet + Pitfall 1 + Pitfall 8)
  </read_first>
  <action>
    (a) `AgentsUsageBarApp.swift`: add `import Sparkle`; add a `private let updaterController: SPUStandardUpdaterController` stored property; in `init()` assign `SPUStandardUpdaterController(startingUpdater: true, updaterDelegate: nil, userDriverDelegate: nil)` (after the existing activation-policy / category-registration lines). Do not add UI for "Check for Updates" in this plan (optional polish; out of scope). Keep all existing scene code intact.
    (b) `Info.plist`: add `<key>SUFeedURL</key>` with value `https://OWNER.github.io/agents_usage_bar/appcast.xml` (placeholder OWNER — Open Question 1; the workflow/Pages owner fills the real handle; document the placeholder in the SUMMARY) and `<key>SUPublicEDKey</key>` with an empty-string placeholder plus an XML comment noting the user pastes the base64 public key from `generate_keys` (per 06-04 / release-setup doc). Change `CFBundleVersion` value to `$(CURRENT_PROJECT_VERSION)` and `CFBundleShortVersionString` to `$(MARKETING_VERSION)` so agvtool can drive both (Pitfall 8). Do NOT remove or alter the NSAppTransportSecurity / NSAllowsLocalNetworking block (REL-08).
    (c) `project.pbxproj`: in BOTH the Debug and Release `XCBuildConfiguration` dicts for the app target, add `CURRENT_PROJECT_VERSION = 1;` and `VERSIONING_SYSTEM = "apple-generic";` (agvtool requires both; they were absent — verified). Leave `MARKETING_VERSION = 0.1.0` as-is (CI tag injection overrides at build time).
  </action>
  <verify>
    <automated>grep -n "import Sparkle\|SPUStandardUpdaterController" AgentsUsageBar/App/AgentsUsageBarApp.swift && grep -n "SUFeedURL\|SUPublicEDKey" AgentsUsageBar/Resources/Info.plist && grep -c "CURRENT_PROJECT_VERSION\|VERSIONING_SYSTEM" AgentsUsageBar.xcodeproj/project.pbxproj && plutil -lint AgentsUsageBar/Resources/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `import Sparkle` and `SPUStandardUpdaterController(startingUpdater: true` both present in `AgentsUsageBarApp.swift`.
    - `Info.plist` contains `SUFeedURL` (https GitHub Pages appcast URL) and `SUPublicEDKey`; `plutil -lint AgentsUsageBar/Resources/Info.plist` reports OK.
    - `Info.plist` still contains `NSAllowsLocalNetworking` set true (REL-08 preserved) — `plutil -extract NSAppTransportSecurity.NSAllowsLocalNetworking raw AgentsUsageBar/Resources/Info.plist` prints `true`.
    - `grep -c "CURRENT_PROJECT_VERSION" AgentsUsageBar.xcodeproj/project.pbxproj` >= 2 (app target Debug + Release).
    - `xcodebuild build -project AgentsUsageBar.xcodeproj -scheme AgentsUsageBar -destination 'platform=macOS' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO` succeeds (Sparkle resolves + links + app compiles with the updater wired).
  </acceptance_criteria>
  <done>App instantiates the Sparkle updater at launch, Info.plist carries the feed + public-key keys and version variables, pbxproj exposes CURRENT_PROJECT_VERSION/VERSIONING_SYSTEM for agvtool; REL-08 untouched; the app builds.</done>
</task>

<task type="auto">
  <name>Task 3: Commit ExportOptions.plist for Developer ID export</name>
  <files>ExportOptions.plist</files>
  <read_first>
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-RESEARCH.md (Pattern 3 — ExportOptions.plist + Open Question 4 DEVELOPMENT_TEAM note)
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-CONTEXT.md (D-03 — Hardened Runtime ON, Sandbox OFF)
  </read_first>
  <action>
    Create `ExportOptions.plist` at the repo root with: `method = developer-id`, `signingStyle = automatic`, `hardenedRuntime = true` (correctly spelled — RESEARCH Pattern 3 has a typo `hardendedRuntime`; use the correct Apple key `hardenedRuntime`), `stripSwiftSymbols = true`, and a `teamID` key set to a documented placeholder `$(DEVELOPMENT_TEAM)` with an XML comment instructing the user to fill their 10-char Apple Developer Team ID (Open Question 4) — the release workflow injects it via the `DEVELOPMENT_TEAM` env/secret if left as a variable. Do NOT enable `uploadSymbols`/`manageAppVersionAndBuildNumber` (we drive versions via agvtool). The plist must be a valid Apple plist.
  </action>
  <verify>
    <automated>plutil -lint ExportOptions.plist && plutil -extract method raw ExportOptions.plist && plutil -extract hardenedRuntime raw ExportOptions.plist</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint ExportOptions.plist` reports OK.
    - `plutil -extract method raw ExportOptions.plist` prints `developer-id`.
    - `plutil -extract hardenedRuntime raw ExportOptions.plist` prints `true` (correctly-spelled key, not the RESEARCH typo).
    - The plist contains a `teamID` entry with a documented placeholder for the user's Team ID.
  </acceptance_criteria>
  <done>A valid Developer-ID ExportOptions.plist exists at repo root for the CI -exportArchive step, with Hardened Runtime on and a documented team-ID placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → app (Sparkle) | The app downloads update DMGs from a remote feed; an attacker controlling the feed or transport could inject a malicious update |
| build config → signed binary | pbxproj/Info.plist values determine what entitlements + runtime hardening the shipped binary carries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Sparkle update download (appcast/DMG) | mitigate | `SUPublicEDKey` pins the EdDSA public key in Info.plist (this plan adds the key slot; 06-04 wires CI signing); Sparkle refuses any DMG whose `sparkle:edSignature` does not verify against the pinned key |
| T-06-04 | Elevation of Privilege | entitlement creep via pbxproj/SPM | mitigate | Entitlements.plist unchanged (D-08); no `disable-library-validation`/`allow-jit` added; assert via `git diff --stat` (acceptance) and the CI entitlement guard in 06-04 |
| T-06-05 | Tampering | Sparkle SPM supply chain | mitigate | Exact version pin `2.9.2` + `Package.resolved` checksum (verified `b83e3743...eed65`) prevents silent dependency drift |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No npm/PyPI/cargo packages; Sparkle is an SPM binary target from the authoritative GitHub repo (RESEARCH Package Legitimacy Audit: Approved, 16+ yr, ~23k stars) |
</threat_model>

<verification>
- `xcodebuild build -scheme AgentsUsageBar -destination 'platform=macOS' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO` succeeds with Sparkle linked.
- `plutil -lint` passes on Info.plist and ExportOptions.plist.
- `NSAllowsLocalNetworking` still true; Entitlements.plist unchanged.
- `CURRENT_PROJECT_VERSION` + `VERSIONING_SYSTEM` present in pbxproj.
</verification>

<success_criteria>
The app embeds Sparkle 2.9.2, instantiates the updater at launch, declares the feed + public-key Info.plist keys, and exposes the version build settings + ExportOptions the CI release pipeline needs — without changing entitlements or REL-08. Maps to ROADMAP Phase 6 Success Criterion #3 (app side).
</success_criteria>

<output>
Create `.planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-02-SUMMARY.md` when done. Record the `SUFeedURL` placeholder OWNER value and the empty `SUPublicEDKey` placeholder so 06-05 / the user knows what to fill in.
</output>
