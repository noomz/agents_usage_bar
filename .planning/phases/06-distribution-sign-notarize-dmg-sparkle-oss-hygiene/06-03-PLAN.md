---
phase: 06-distribution-sign-notarize-dmg-sparkle-oss-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - SECURITY.md
  - README.md
  - docs/entitlements.md
  - docs/release-setup.md
autonomous: true
requirements: [REL-07]

must_haves:
  truths:
    - "The repo ships an MIT LICENSE at root."
    - "README states an explicit privacy promise: no telemetry, no crash-reporter uploads (SEC-05 cross-ref)."
    - "docs/entitlements.md justifies the single network.client entitlement and the NSAllowsLocalNetworking ATS flag."
    - "docs/release-setup.md names the exact GitHub secret keys and gives copy-paste commands to provision the Developer ID cert, ASC API key, and Sparkle EdDSA key."
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "SECURITY.md"
      provides: "vulnerability disclosure policy + contact"
      contains: "Security"
    - path: "docs/entitlements.md"
      provides: "per-entitlement + ATS justification"
      contains: "com.apple.security.network.client"
    - path: "docs/release-setup.md"
      provides: "secret provisioning runbook with exact secret names"
      contains: "DEVELOPER_ID_CERT_P12"
  key_links:
    - from: "docs/release-setup.md"
      to: ".github/workflows/release.yml"
      via: "the exact secret names the workflow reads"
      pattern: "ASC_API_KEY_P8"
---

<objective>
Ship OSS-hygiene docs (REL-07) and the credential-provisioning runbook the deferred release pipeline depends on (D-02): MIT `LICENSE`, a privacy-promising `README.md` (SEC-05 cross-ref), `SECURITY.md`, `docs/entitlements.md` (entitlement + ATS justification), and `docs/release-setup.md` (exact GitHub secret names + export/create commands).

Purpose: Make the repo trustworthy and open-source-ready, and unblock the human-gated credential setup (06-05) by giving the user an unambiguous, copy-paste provisioning guide naming the exact secrets the workflow consumes.
Output: Five markdown files. Text/badges are autonomous; real captured screenshots are deferred to 06-05 (placeholder reference in README for now).
</objective>

<execution_context>
@$HOME/.ccs/instances/personal/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-CONTEXT.md
@.planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-RESEARCH.md
@README.md

<interfaces>
<!-- Authoritative facts these docs must state correctly. -->

From RESEARCH.md "Secret Names" table — the EXACT secret keys release.yml reads (06-04):
- DEVELOPER_ID_CERT_P12 (base64 .p12), DEVELOPER_ID_CERT_PASSWORD, ASC_API_KEY_P8 (raw .p8 content), ASC_API_KEY_ID (10-char), ASC_API_ISSUER_ID (UUID), SPARKLE_ED_PRIVATE_KEY.
- Plus DEVELOPMENT_TEAM (Open Question 4) used by ExportOptions.plist.

From the verified Entitlements.plist: only `com.apple.security.network.client` (REL-09).
From the verified Info.plist: `NSAppTransportSecurity → NSAllowsLocalNetworking = true` (REL-08, for localhost provider probes).

From CLAUDE.md + CONTEXT D-01: License = MIT, copyright holder = repo owner (`lazym0m3nt@gmail.com` per Info.plist NSHumanReadableCopyright). Privacy: no telemetry, no crash reporter (SEC-05).
RESEARCH "Code Examples → GitHub secret setup" + "notarytool submit" give the exact `security export` / `base64` / `notarytool` / `generate_keys` commands to reproduce in release-setup.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MIT LICENSE + SECURITY.md</name>
  <files>LICENSE, SECURITY.md</files>
  <read_first>
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-CONTEXT.md (D-01 license + D-10 SECURITY.md scope)
    - AgentsUsageBar/Resources/Info.plist (NSHumanReadableCopyright — copyright holder / year)
  </read_first>
  <action>
    Create `LICENSE` at repo root containing the standard MIT License text, with copyright line `Copyright (c) 2026 lazym0m3nt@gmail.com` (matching the Info.plist NSHumanReadableCopyright holder/year). Use the canonical MIT wording verbatim (the standard permission + warranty-disclaimer paragraphs) — no modifications.
    Create `SECURITY.md` with: a "Supported Versions" note (latest release line supported), a "Reporting a Vulnerability" section instructing private disclosure to the maintainer contact (email lazym0m3nt@gmail.com or GitHub private security advisory), an expected acknowledgment window, and a one-line scope note (this is a local-only menu bar utility; no server component). Keep it concise and accurate; do NOT promise SLAs the project cannot meet.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "Permission is hereby granted" LICENSE && test -f SECURITY.md && grep -qi "Reporting a Vulnerability" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` succeeds; file contains `MIT License`, `Permission is hereby granted`, and the 2026 copyright line.
    - `test -f SECURITY.md` succeeds; contains a "Reporting a Vulnerability" section with a disclosure contact.
  </acceptance_criteria>
  <done>MIT LICENSE and SECURITY.md exist at repo root with correct, canonical content.</done>
</task>

<task type="auto">
  <name>Task 2: docs/entitlements.md + docs/release-setup.md</name>
  <files>docs/entitlements.md, docs/release-setup.md</files>
  <read_first>
    - AgentsUsageBar/Resources/Entitlements.plist (the single entitlement to justify)
    - AgentsUsageBar/Resources/Info.plist (NSAllowsLocalNetworking ATS flag to justify)
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-RESEARCH.md ("Secret Names" table + "Code Examples" for cert export / ASC key / generate_keys; Pattern 2 secret references)
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-CONTEXT.md (D-02 setup-doc requirement + D-08 entitlements + D-10 docs/entitlements.md)
  </read_first>
  <action>
    Create `docs/entitlements.md` explaining EACH declared entitlement and ATS flag and WHY: (1) `com.apple.security.network.client` — required for remote provider HTTPS calls (OpenRouter/Anthropic/OpenAI/Google) AND localhost probes; (2) `NSAppTransportSecurity → NSAllowsLocalNetworking = true` — required so the app can reach `http://localhost:{11434,1234,...}` for Ollama/LM Studio/llama.cpp without ATS blocking cleartext-localhost. Explicitly state what is NOT present and why: App Sandbox OFF (DMG distribution; needs to read `~/.claude` etc. — cite CLAUDE.md Sandbox Decision), and NO `disable-library-validation`/`allow-jit`/`allow-dyld-environment-variables` (Sparkle 2.x on non-sandboxed Hardened Runtime does not need them — per RESEARCH).
    Create `docs/release-setup.md` as a step-by-step runbook that: lists a table of the EXACT secret names (`DEVELOPER_ID_CERT_P12`, `DEVELOPER_ID_CERT_PASSWORD`, `ASC_API_KEY_P8`, `ASC_API_KEY_ID`, `ASC_API_ISSUER_ID`, `SPARKLE_ED_PRIVATE_KEY`, plus `DEVELOPMENT_TEAM`) with content + how-to-obtain; gives copy-paste commands to (a) export the Developer ID Application cert to `.p12` and base64-encode it (`security export ... -f pkcs12` then `base64 -i`), (b) create + download the ASC API key `.p8` (App Store Connect → Users and Access → Integrations → Keys) and where to find Key ID + Issuer ID, (c) generate the Sparkle EdDSA key pair (`./bin/generate_keys` then `-x` to export the private key) and where the PUBLIC key goes (`SUPublicEDKey` in Info.plist — cross-ref 06-02) vs the PRIVATE key (the `SPARKLE_ED_PRIVATE_KEY` secret), (d) load each value as a GitHub repository secret (`gh secret set NAME < file` or web UI), and (e) enable GitHub Pages (Settings → Pages → Deploy from branch → `gh-pages`) which 06-04 scaffolds. State clearly these are performed ONCE by the user and the release workflow consumes the named secrets unattended thereafter (D-02). Use the verified commands from RESEARCH; do not invent flags. Use obvious placeholders only — never paste real key material or any literal `sk-`/`AIza` token.
  </action>
  <verify>
    <automated>test -f docs/entitlements.md && grep -q "com.apple.security.network.client" docs/entitlements.md && grep -q "NSAllowsLocalNetworking" docs/entitlements.md && test -f docs/release-setup.md && grep -q "DEVELOPER_ID_CERT_P12" docs/release-setup.md && grep -q "ASC_API_KEY_P8" docs/release-setup.md && grep -q "SPARKLE_ED_PRIVATE_KEY" docs/release-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/entitlements.md` exists; names `com.apple.security.network.client` AND `NSAllowsLocalNetworking`; explains the sandbox-off rationale and the absence of disable-library-validation/allow-jit.
    - `docs/release-setup.md` exists; contains ALL six secret names plus DEVELOPMENT_TEAM, with provisioning commands for cert export, ASC key creation, and Sparkle generate_keys.
    - No literal `sk-`/`AIza` token strings (placeholders only).
  </acceptance_criteria>
  <done>Entitlement justification and the credential-provisioning runbook exist; secret names match exactly what 06-04's workflow will read.</done>
</task>

<task type="auto">
  <name>Task 3: README — MIT badge, install, privacy promise (SEC-05)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 69-line state — extend, do not blow away existing content)
    - .planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-CONTEXT.md (D-10 README scope)
    - .planning/REQUIREMENTS.md (SEC-05 privacy text at line 106)
  </read_first>
  <action>
    Update `README.md` to add (preserving existing content): an MIT license badge + a License section referencing `LICENSE` (D-01/REL-07); an Install section describing the notarized-DMG download from GitHub Releases and that the app auto-updates via Sparkle; an explicit Privacy section stating verbatim-in-spirit "No telemetry. No analytics. No crash-reporter uploads. All data stays on your machine." (SEC-05 cross-ref) and a one-line pointer to `docs/entitlements.md` for the why-unsandboxed explanation; a Screenshots section with a placeholder (Markdown image reference to a `docs/screenshots/` path that 06-05 will populate with a real capture — add an HTML comment marking it as awaiting the human-gated capture in 06-05). Do NOT fabricate a screenshot. Keep the README accurate to the shipped feature set (Claude/Codex/Gemini/OpenRouter + local LLM presence rows).
  </action>
  <verify>
    <automated>grep -qi "MIT" README.md && grep -qi "no telemetry\|No telemetry" README.md && grep -qi "privacy" README.md && grep -q "docs/entitlements.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains an MIT badge/section, an Install section, and a Privacy section asserting no telemetry + no crash-reporter uploads (SEC-05).
    - README links to `docs/entitlements.md`.
    - A screenshot placeholder is present, marked as awaiting the 06-05 human-gated capture (no fabricated image).
    - Existing README content is preserved (line count grows, prior sections retained).
  </acceptance_criteria>
  <done>README is OSS-ready: MIT badge, install instructions, an explicit privacy/no-telemetry promise, and a screenshot placeholder deferred to 06-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user actions | The release-setup runbook instructs the user to handle live secrets; wrong instructions could leak credentials or misconfigure least-privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | release-setup.md leaks key material | mitigate | Doc uses placeholders only; instructs `gh secret set` / web UI (never committing keys); instructs `rm -f` of temp p12/p8 after encoding (per RESEARCH Pitfall 5) |
| T-06-07 | Repudiation / Spoofing | README privacy claims unverifiable | mitigate | Privacy promise is backed by SEC-05 (no telemetry SDK in the codebase) and docs/entitlements.md shows the single network.client entitlement scope |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (markdown only) |
</threat_model>

<verification>
- `test -f LICENSE && test -f SECURITY.md && test -f docs/entitlements.md && test -f docs/release-setup.md` all succeed.
- README grep gates (MIT, no-telemetry, privacy, docs/entitlements.md link) pass.
- No literal `sk-`/`AIza` strings introduced.
</verification>

<success_criteria>
REL-07 satisfied: MIT LICENSE, README (privacy promise + install + screenshot placeholder), SECURITY.md, and docs/entitlements.md all present and accurate; the D-02 release-setup runbook names the exact secrets unblocking 06-05. Maps to ROADMAP Phase 6 Success Criterion #4.
</success_criteria>

<output>
Create `.planning/phases/06-distribution-sign-notarize-dmg-sparkle-oss-hygiene/06-03-SUMMARY.md` when done.
</output>
