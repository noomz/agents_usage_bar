import Testing
@testable import AgentsUsageBar
import Foundation

// Plan 03-08 Task 2 — placeholder; tests added in subsequent task.
// Empty suite so the pbxproj entry compiles cleanly while Task 1 runs first.

@Suite("AggregateStoreGeminiDegradedSuppressionTests")
struct AggregateStoreGeminiDegradedSuppressionTests {
    @Test("placeholder until Task 2 lands")
    func placeholder() {
        #expect(true)
    }
}
